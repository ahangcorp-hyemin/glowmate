# 동기화 중간 산출물
.tmp/

# 파생물 — docs/tasks/*.md 계약에서 생성된다
# (CI 재현성을 위해 커밋해 두되, 손으로 고치지 말 것)
# docs/tasks.json

# OS
.DS_Store

# Node
node_modules/
.next/
out/
dist/
*.tsbuildinfo
.env*.local

# Python
__pycache__/
*.py[cod]
.venv/
venv/
