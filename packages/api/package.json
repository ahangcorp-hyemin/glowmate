{
  "name": "@glowmate/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo '@glowmate/api 는 타입 전용 스캐폴드다 — 산출물이 없다. 타입 검사는 typecheck job 이 담당한다'",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --test --experimental-strip-types"
  },
  "devDependencies": {
    "@types/node": "24.10.1"
  }
}
