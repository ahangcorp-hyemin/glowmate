# 실제 값을 이 파일에 적지 마라 — secret-scan job 이 리포 전체를 검사한다.
# 로컬 값은 .env.local 에 둔다 (.gitignore 의 .env*.local 로 제외됨).

# packages/db — readDbConfig() 가 읽는다
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME
DB_MAX_CONNECTIONS=5

# apps/web
NEXT_PUBLIC_SITE_URL=http://localhost:3000
