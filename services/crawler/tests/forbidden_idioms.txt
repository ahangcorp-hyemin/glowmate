# F1 FORBID-2 패턴 사전 (services/crawler 한정)
#
# 이 파일은 **검사기의 패턴 정의 파일**이다. F1 FORBID-2 의 when 절이
# "검사기의 패턴 정의 파일은 대상에서 제외한다"고 명시한 그 파일이며,
# 리포 전역 diff 스캐너(CI job `lint`)의 제외 목록에 등재되어야 한다.
#
# 형식: 한 줄에 금지 문자열 1개. `#` 로 시작하는 줄과 빈 줄은 무시한다.
# 판정 기준은 열거가 아니라 "종료 코드 마스킹 또는 검사 대상 축소"라는 성질이며,
# 아래는 그 성질에 해당하는 관용구 목록이다.

continue-on-error: true
|| true
|| :
set +e
--passWithNoTests
--continue-on-collection-errors
@pytest.mark.skip
@pytest.mark.xfail
pytest.skip(
pytest.xfail(
pytestmark = pytest.mark.skip
if: always()
.only(
describe.skip
it.skip
