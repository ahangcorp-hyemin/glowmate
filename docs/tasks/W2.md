# W2 — 탐색(리스트 + 니즈 태그 필터)

> 개정 2026-08-03 — `docs/audit/W1-O3-audit.md` W2 항목 및 `03-task-dag.md` 정정 #1·#2·#5·#9 반영.
> 주요 변경: `lib/price-state.ts` 자체 생성 폐기 → F6-PRICE-STATE 의존 · `is_public` → `visibility` ENUM · 데이터 취득을 F5 경유로 고정 · 가격/medical 렌더 하한 REQ 신설(원칙 2.5) · 실환경 하한 REQ 신설(R-7) · DS5/DS6 실제 산출물명으로 교정.

```yaml
# ─── 식별 ───────────────────────────────
id:            W2-DISCOVERY-LIST
title:         "탐색 화면 — 업체 리스트 + 3050 니즈 태그 필터"
workstream:    web
owner_agent:   dev-web

# ─── 존재 이유 ──────────────────────────
traces_to:     [H5, S2]
why:           "니즈 태그로 좁혀 보는 표면이 없으면 glowmate는 네이버 지도 검색의 열등한 복제본이 되고, 차별점 가설 H5를 관측할 화면 자체가 존재하지 않는다."

# ─── DAG ────────────────────────────────
depends_on:    [W1-SEO-FOUNDATION, F2a-CORE-SCHEMA, F2b-QUALITY-SCHEMA, F4-NEED-TAG-ONTOLOGY, F5-API-LAYER, F6-PRICE-STATE, C4-PRICE-NORMALIZER, C7-QUALITY-GATE, D4-MEDICAL-AD-GUARDRAIL, DS5-CORE-COMPONENTS, DS6-A11Y-GATE]
blocks:        [W4-COMBO-LANDING, W6-LEAD-TRACKING]
parallel_with: [W3-VENUE-DETAIL, W7-CORRECTION-REQUEST]
gate:          [G1, G2, G3]

# ─── 산출물 ─────────────────────────────
deliverable:
  pr_count:    1
  touches:
    - apps/web/src/app/(browse)/**                 # W1 매니페스트가 도출하는 리스트 라우트 세그먼트
    - apps/web/src/app/api/venues/route.ts         # @glowmate/api 위임 전용 얇은 핸들러
    - apps/web/src/components/browse/**
    - apps/web/src/lib/filters/**
    - apps/web/src/lib/sitemap-sources/browse.ts   # W1 규약에 따른 제공자 추가 (sitemap.ts 수정 금지)
    - apps/web/tests/browse/**
    - apps/web/fixtures/browse/**
    - apps/web/package.json                        # scripts 항목 추가만
  artifacts:
    - "리스트 라우트(SSG/ISR, revalidate ≤ 3600) + 페이지네이션 20건"
    - "니즈 태그 필터 UI(DS5 FilterChip·VenueCard 사용) + URL 쿼리 직렬화 모듈"
    - "GET /api/venues 얇은 핸들러 + zod 응답 스키마"
    - "정렬 규칙(회당 단가 오름차순 · 비확정 후순위) 단위테스트"
    - "가격/medical 렌더 커버리지 스모크 스크립트"

# ─── 요구사항 ───────────────────────────
requirements:
  - id: REQ-1
    statement: "리스트 라우트는 JS 비활성 상태의 첫 HTML 에 상위 20개 카드의 업체명 20건과 data-price-state 속성 20건을 포함한다."
    acceptance: "`pnpm --filter @glowmate/web test:browse-ssr` — JS 비활성 fetch HTML 에서 픽스처 업체명 20건·속성 20건 미만이면 실패."
  - id: REQ-2
    statement: "필터에 사용 가능한 니즈 태그 집합은 F4 온톨로지가 export 한 집합과 대칭차집합 0 이다."
    acceptance: "`pnpm --filter @glowmate/web test:filters` — 대칭차집합 1건 이상이면 실패."
  - id: REQ-3
    statement: "태그를 2개 이상 선택한 경우 응답 항목은 선택 태그를 모두 보유한 업체로만 구성된다(AND 조건)."
    acceptance: "`pnpm --filter @glowmate/web test:filters-and` — 무작위 조합 100회에서 조건 미충족 항목이 1건이라도 반환되면 실패."
  - id: REQ-4
    statement: "필터·정렬·페이지 상태가 URL 쿼리에 반영되어 동일 URL 재요청 시 서버 렌더 결과의 업체 ID 배열이 동일하다."
    acceptance: "`pnpm --filter @glowmate/web test:e2e:browse` — 필터 3개 적용 URL 을 새 세션에서 재요청해 ID 배열 정확 일치."
  - id: REQ-5
    statement: "기본 정렬은 회당 단가 오름차순이며, @glowmate/price-state 판정이 confirmed 가 아닌 항목은 confirmed 항목 전체보다 뒤에 배치되고 동일 키에서 안정 정렬을 만족한다."
    acceptance: "`pnpm --filter @glowmate/web test:sort` — 50건 픽스처(confirmed/estimated/conflicted/unknown 혼합) 출력 ID 순서 스냅샷 일치."
  - id: REQ-6
    statement: "필터·정렬·페이지 쿼리가 1개 이상 포함된 URL 은 robots meta 에 noindex 를 출력한다."
    acceptance: "`pnpm --filter @glowmate/web test:browse-meta` — 쿼리 포함 URL 4종에 noindex 가 없으면 실패."
  - id: REQ-7
    statement: "C7 public_venue 스테이징 스냅샷 기준 리스트 첫 페이지 20건에서, 숫자 회당 단가가 렌더된 카드 수가 그 20건 중 price_state=confirmed 인 업체 수와 정확히 일치한다(미표기 누락 0건)."
    acceptance: "`pnpm --filter @glowmate/web smoke:browse-price --env=staging` — 불일치 1건 이상이면 종료 코드 1. 모집단은 스냅샷 전량, 대상 20건은 REQ-5 정렬 규칙으로 결정되며 별도 표집을 하지 않는다(R-6 비적용). (FORBID-2의 짝 — 전 항목을 '가격 미공개'로 렌더하면 실패한다)"
  - id: REQ-8
    statement: "동일 스냅샷에서 category='medical_wellness' 업체의 리스트 노출 누락률이 0이며, D4 display_whitelist 에 매칭되는 가격 표기를 보유한 medical 업체의 가격 렌더 비율이 100% 이다."
    acceptance: "`pnpm --filter @glowmate/web smoke:browse-medical --env=staging` — 노출 누락 1건 또는 허용 포맷 미렌더 1건 이상이면 종료 코드 1. (FORBID-6의 짝 — medical 축을 통째로 숨기면 실패한다)"

# ─── 조건부 금지사항 ────────────────────
forbid:
  - id: FORBID-1
    when:         "apps/web/src/app/api/venues/route.ts 또는 (browse) 세그먼트가 업체 데이터를 취득할 때"
    must_not:     "@glowmate/api 의 공개 조회 함수 외의 경로(packages/db 직접 import · SQL 문자열 · visibility 필터 자체 구현 · includeHidden/preview/debug 파라미터 전달)를 사용"
    because:      "공개 판정이 화면마다 재구현되면 한 곳만 조건을 빠뜨려도 hidden_quality·hidden_request 업체가 리스트에 노출되고, 그 카드가 색인·공유되는 순간 되돌릴 수 없다. F5가 함수를 제공하지 않으면 대기해야지 자체 쿼리로 대체해서는 안 된다."
    detect:       "F1 boundary job(dependency-cruiser: apps/web → packages/db 금지) + 통합테스트 — visibility 4종 픽스처와 파라미터 fuzz 20종에서 public 외 레코드가 응답에 등장하면 실패."
    on_violation: block_merge
  - id: FORBID-2
    when:         "카드 항목의 @glowmate/price-state 판정이 confirmed 가 아닌 경우(estimated·conflicted·unknown)"
    must_not:     "confirmed 카드와 동일한 시각 표현으로 가격을 노출하거나, 최저가·평균가 집계 라벨의 계산 입력에 포함"
    because:      "추정치가 확정가와 섞여 정렬·최저가 배지에 반영되면 사용자는 잘못된 업체를 1순위로 클릭하고, 실제 문의에서 금액이 다르다는 경험 한 번이 UVP 전체인 '가격 신뢰'를 무너뜨린다."
    detect:       "단위테스트 — 상태 4종 픽스처의 data-price-state 값 검증 + 최저가 계산 함수가 confirmed 외 입력을 받으면 throw. 짝 REQ-7이 반대 방향(전량 숨김)을 막는다."
    on_violation: block_merge
  - id: FORBID-3
    when:         "응답 배열에 요청 파라미터(니즈 태그·지역·카테고리·가격 범위)를 만족하지 않는 항목이 포함되는 경우"
    must_not:     "결과 수가 적다는 이유로 조건을 서버·클라이언트에서 완화해 해당 항목을 결과 목록 배열에 병합"
    because:      "'여성전용·평일오전'으로 걸렀는데 조건 미충족 업체가 목록에 섞이면 사용자는 조건 충족으로 오해해 방문하고, H5 지표도 실제 유용성과 무관한 값이 되어 가설 판정이 오염된다."
    detect:       "속성 테스트 — 무작위 파라미터 조합 100회에 대해 응답 전 항목이 전 조건을 만족하는지 검증(완화 제안은 결과 배열이 아닌 data-section=\"suggestions\" 로만 반환)."
    on_violation: block_merge
  - id: FORBID-4
    when:         "가격 확정 판정 임계 또는 3050 접근성 임계(폰트·터치타깃·대비) 수치를 W2 touches 내 파일에 기입할 때"
    must_not:     "@glowmate/price-state 및 DS 토큰 패키지가 export 하는 값 외에 동일 임계 수치를 리터럴로 재선언"
    because:      "임계가 두 곳에 존재하면 F6나 DS가 값을 바꿔도 리스트만 옛 기준으로 confirmed 를 판정하거나 옛 폰트 크기를 통과시키고, 그 불일치는 화면에서 보이지 않아 아무도 발견하지 못한다."
    detect:       "저장소 전체(apps/**, packages/**) 스캔 스크립트 — 해당 임계 리터럴 선언이 정본 2곳(@glowmate/price-state, DS 토큰 패키지) 외에서 검출되면 종료 코드 1."
    on_violation: block_merge
  - id: FORBID-5
    when:         "필터·정렬·페이지 파라미터가 1개 이상 포함된 URL 을 다룰 때"
    must_not:     "해당 URL 을 sitemap 제공자에 포함하거나 noindex 없이 색인 가능 상태로 노출"
    because:      "태그 8종 조합만으로 수백 URL 이 생성되고, 내용이 거의 같은 페이지가 대량 색인되면 크롤 예산이 소진되어 W4의 롱테일 페이지(H3의 본체)가 색인되지 않는다."
    detect:       "테스트 — 쿼리 포함 URL 4종의 noindex 존재 + browse.ts 제공자 출력에 쿼리 포함 URL 0건 검증."
    on_violation: block_merge
  - id: FORBID-6
    when:         "카드의 category 가 medical_wellness 인 경우"
    must_not:     "시술·진료 항목명과 금액을 한 문자열로 결합해 표기하거나 docs/discovery/D4/forbidden_lexicon.yaml 등재 표현을 렌더"
    because:      "비급여 진료비의 비교·유인 표시는 의료광고법 규제 대상이며, 리스트는 노출량이 가장 많은 표면이라 단일 신고가 전 카드로 확대된다."
    detect:       "렌더 스캔 — medical 픽스처 20건(금칙 표현 포함 10건) HTML 을 forbidden_lexicon.yaml 정규식으로 매칭해 1건이라도 걸리면 실패. 짝 REQ-8이 전량 숨김을 막는다."
    on_violation: block_merge

# ─── 경계 ───────────────────────────────
out_of_scope:
  - "@glowmate/price-state 모듈 자체 생성·수정 — F6-PRICE-STATE 소유. 없으면 착수 대기(자체 판정 로직 작성 금지)"
  - "공개 조회 함수·visibility 필터 구현 — F5-API-LAYER 소유"
  - "3050 접근성 임계 검사 구현 — DS6-A11Y-GATE 의 CI 게이트에 위임"
  - "필터 사용 계측 이벤트(filter_applied) — W9-OBSERVABILITY-EVENTS 소유. W2는 이벤트 발화 지점의 data 속성만 제공"
  - "업체 상세·가격 비교 블록(W3) · 조합 랜딩(W4)"
  - "W1 의 URL 규칙·canonical 빌더·sitemap.ts·robots 수정 (제공자 파일 추가만 허용)"
  - "지도 뷰·반경 검색 UI"
rollback:      "PR 1건 revert. 리스트 라우트·핸들러·제공자 파일이 모두 본 PR 신규 추가분이므로 revert 후 리스트 URL 은 W1 규약에 따라 404 를 반환하고 sitemap 에서 제외된다. 공유 모듈을 생성하지 않으므로 W3 컴파일에 영향이 없다. DB 변경 없음."
done_when:
  - "REQ-1~REQ-8 의 acceptance 명령이 전부 통과하고 apps/web package.json scripts 에 등록됨"
  - "REQ-7·REQ-8 스테이징 스모크 로그(스냅샷 업체 수·confirmed 수·렌더 수)가 PR 본문에 첨부됨"
  - "W1 의 check-render-strategy 가 리스트 라우트에 대해 통과(revalidate ≤ 3600)"
  - "FORBID-1~6 각각의 탐지 수단이 의도적 위반 픽스처에서 실패하는 것을 확인한 기록이 PR 본문에 첨부"
  - "픽스처가 C7 public_venue 뷰 컬럼 구성 및 F2 visibility ENUM 4값과 zod 로 일치 검증됨"
```
