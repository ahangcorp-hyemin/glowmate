# F6 — 가격 상태 공용 모듈 (확정 / 추정 / 충돌 / 미확정 판정 + 표시 규칙)

> 계약 규격: [02-task-contract-spec.md](../02-task-contract-spec.md) §1
> 신설: 2026-08-03 (`docs/audit/W1-O3-audit.md` — `lib/price-state.ts` 를 W2·W3 가 공유해 PR 1개 원칙 위반)

```yaml
# ─── 식별 ───────────────────────────────
id:            F6-PRICE-STATE
dag_id:        F6
title:         가격 상태 공용 모듈 (price_state 판정 · 임계 단일 소스 · 표시 규칙)
workstream:    foundation
owner_agent:   dev-web

# ─── 존재 이유 ──────────────────────────
traces_to:     [H4, S1, KM-price-coverage]
why:           "확정/추정/충돌/미확정 판정이 표면마다 따로 구현되면 같은 업체가 리스트에서는 확정,
                상세에서는 추정으로 보이고 '가격이 틀린 사이트' 평판이 만들어진다."

# ─── DAG ────────────────────────────────
depends_on:    [F2b-QUALITY-SCHEMA, C4-PRICE-NORMALIZER]
blocks:        [W2-DISCOVERY-LIST, W3-VENUE-DETAIL, W4-COMBO-LANDING, W5-EDITOR-REPORT]
parallel_with: [C5-ENTITY-RESOLUTION, C6-TAG-ASSIGN]
gate:          [G1, G3]              # C4 의존이므로 Phase 2 착수 조건을 승계한다

# ─── 산출물 ─────────────────────────────
deliverable:
  pr_count:    1
  touches:
    - packages/price-state/src/**
    - packages/price-state/test/**
    - packages/price-state/fixtures/**
    - packages/price-state/package.json
    - tools/check-price-state-single-source.mjs
    - .github/workflows/ci.yml                # price-state job 추가만
    - .github/ci-budget.json                  # price-state 예산 항목 추가만
  artifacts:
    - "classifyPriceState() — 4상태 판정 순수 함수"
    - "PRICE_CONFIDENCE_MIN 상수 (리포지토리 단일 선언)"
    - "상태별 표시 규칙(showNumeric · badgeKey · reasonKey) 테이블"
    - "결정표 문서 + 골든 판정 셋 (개발 60 / 홀드아웃 40)"
    - "리포지토리 전역 단일 소스 검사 스크립트"

# ─── 요구사항 ───────────────────────────
requirements:
  - id: REQ-1
    statement: >
      classifyPriceState(input) 이 confirmed · estimated · conflicted · unknown 중 정확히 1개를 반환하며,
      판정 우선순위가 unknown > conflicted > estimated > confirmed 로 고정된다
      (unknown: price_per_session IS NULL 또는 confidence IS NULL / conflicted: 미해소 price_conflict 존재 /
       estimated: confidence < PRICE_CONFIDENCE_MIN / confirmed: 그 외).
    acceptance: "`pnpm test:price-state` — 결정표 전수 케이스(3개 조건 × null 포함 = 24행) 각각의 기대 상태 일치, 속성 테스트 500케이스에서 반환값이 4상태 밖이거나 복수인 건수 0."

  - id: REQ-2
    statement: "PRICE_CONFIDENCE_MIN = 0.70 이 packages/price-state/src/constants.ts 에 1회만 선언되고, 리포지토리 전체에서 confidence 비교 연산과 동일 임계 리터럴의 타 위치 선언이 0건이다."
    acceptance: "CI job `price-state` — `node tools/check-price-state-single-source.mjs` 가 packages/ui·apps/web·packages/api 를 포함한 전 경로를 스캔해 위반 1건이라도 있으면 exit 1 (검사 범위에서 제외된 경로가 있으면 스크립트가 목록을 출력)."

  - id: REQ-3
    statement: >
      상태별 표시 규칙 함수가 4상태 전부에 대해 (showNumeric: boolean, badgeKey: string, reasonKey: string)
      3필드를 반환하고, showNumeric 이 true 인 상태는 confirmed 하나뿐이다.
    acceptance: "`pnpm test:price-state` — 4상태 반환값 3필드 non-empty assert, showNumeric===true 인 상태 집합이 {confirmed} 와 정확히 일치."

  - id: REQ-4
    statement: >
      홀드아웃 판정 셋 40건(confidence·conflict·null 조합이 층화된 실 파이프라인 출력)에 대해
      기대 상태와 불일치 건수가 0 이다.
    acceptance: "`pnpm test:price-state-holdout` — mismatch 카운트 assertion === 0."

  - id: REQ-5
    statement: >
      고신뢰 골든 입력 40건(price_per_session NOT NULL · confidence ≥ 0.70 · conflict 없음) 전건이
      confirmed 로 판정되고 표시 규칙이 showNumeric=true 를 반환한다 (정상 표시 하한).
    acceptance: "`pnpm test:price-state` — 40건 중 confirmed 아닌 건 또는 showNumeric=false 가 1건이라도 있으면 exit 1."

  - id: REQ-6
    statement: >
      평가 데이터는 구현자가 창작하지 않는다 — 골든/홀드아웃 100건은 C4 가 실데이터로 산출한
      price_plan 행 모집단에서 (상태 조건 × 3축) 층화 후 고정 시드 무작위 추출하며,
      홀드아웃 40건은 별도 파일에 sha256 잠금된 상태로 개발 중 열람하지 않는다.
    acceptance: "`pnpm test:price-state-holdout` — 추출 스크립트 재실행 결과가 커밋된 셋과 동일(시드 재현성), holdout.lock 의 sha256 일치, 홀드아웃 파일이 개발셋 테스트에서 import 되면 exit 1."

  - id: REQ-7
    statement: >
      스테이징 DB 의 실제 price_plan 표본 300행에 대해 판정 분포를 산출했을 때 4상태 각각 1건 이상이고
      confirmed 비율이 30% 이상이다 (실환경 하한).
    acceptance: "`pnpm smoke:price-state` — 분포 리포트를 stdout 에 출력하고, 조건 미달 시 exit 1. 표본이 300행 미만이면 `insufficient_sample` 로 exit 1."

# ─── 조건부 금지사항 ────────────────────
forbid:
  - id: FORBID-1
    when: "입력의 confidence 가 null 또는 undefined 인 경우"
    must_not: "기본값(1.0 등)으로 대체하거나 `?? 1` · `|| 1` 로 보정해 confirmed 로 판정"
    because: >
      C4 가 confidence 를 채우지 못한 행이 전부 확정 가격으로 렌더된다. 사용자는 그 숫자를 믿고 전화를 걸고,
      실제 가격이 다르면 UVP 전체가 걸린 가격 신뢰가 무너진다. 파이프라인이 조용히 실패한 사실도 함께 은폐된다.
    detect: "`pnpm test:price-state` — confidence null/undefined 입력 12케이스가 전부 unknown 임을 assert + 소스에서 `confidence ?? `·`confidence || ` 패턴 검출 시 exit 1"
    on_violation: block_merge

  - id: FORBID-2
    when: "packages/price-state 밖의 파일(apps/web/src/** · packages/ui/** · packages/api/**)에 판정 분기 또는 confidence 임계 리터럴을 선언하는 경우"
    must_not: "판정 로직·임계값을 소비 측에서 재선언 (import 만 허용)"
    because: >
      W2 리스트와 W3 상세가 각자 임계를 갖게 되면 같은 업체가 화면마다 다른 신뢰도로 표시된다.
      한쪽만 갱신되는 순간부터 어느 화면이 맞는지 판정할 근거가 사라진다.
    detect: "CI job `price-state` — `check-price-state-single-source.mjs` 전 경로 스캔(REQ-2) + 위반 픽스처 `packages/price-state/fixtures/violation-duplicate-threshold.ts` 가 반드시 실패하는지 확인하는 메타테스트"
    on_violation: block_merge

  - id: FORBID-3
    when: "REQ-4·REQ-5 테스트가 실패한 상태에서 PRICE_CONFIDENCE_MIN 값 또는 골든/홀드아웃 셋의 기대 라벨을 변경하는 경우 (base 에 파일이 없는 최초 도입은 제외)"
    must_not: "임계 하향 또는 기대 라벨 수정으로 통과시키는 것"
    because: >
      임계를 0.3 으로 낮추면 추정가가 전부 확정으로 승격되어 W2·W3·W4 의 3중 방어가 상수 한 줄로 동시에 무력화된다.
      그 뒤에는 화면의 '확정' 배지가 무엇을 의미하는지 아무도 말할 수 없다.
    detect: "CI job `price-state` — constants.ts·fixtures/**·holdout.lock 의 diff 감지 시 packages/price-state CODEOWNERS 승인 리뷰(승인자 ≠ PR 작성자, GitHub API 조회) 없이는 exit 1"
    on_violation: block_merge

  - id: FORBID-4
    when: "판정 결과가 confirmed 가 아닌 입력에 대해 표시 규칙을 산출할 때"
    must_not: "showNumeric=true 반환, 또는 최저가·평균·범위 같은 숫자 파생값을 함께 반환"
    because: >
      W2 카드·W3 상세·W4 통계가 모두 이 모듈의 출력만 믿고 렌더한다. 여기서 한 번 뚫리면 세 표면이
      동시에 추정치를 확정 가격으로 노출하며, 각 표면의 테스트는 전부 초록으로 남는다.
    detect: "`pnpm test:price-state` — 속성 테스트 500케이스에서 (state !== 'confirmed') → (showNumeric === false && 숫자 파생 필드 부재) 위반 0건"
    on_violation: block_merge

  - id: FORBID-5
    when: "미해소 price_conflict 가 존재하는 입력(conflicted)을 처리할 때"
    must_not: "한쪽 값을 골라 단일 대표값 필드로 반환하거나, 두 값의 평균·최소값을 산출"
    because: >
      단독 노출은 '평균값 금지' 검사(렌더 숫자가 입력값의 부분집합인지)를 통과하기 때문에 소비 측에서
      영원히 탐지되지 않는다. 근거 없는 한쪽 선택이 그대로 화면 가격이 되고, 반대쪽 값을 본 사용자는
      가격이 틀렸다고 판단한다.
    detect: "`pnpm test:price-state` — conflicted 픽스처 10건에서 반환 객체가 values 배열(길이 ≥2)만 갖고 representative/primary 계열 필드가 존재하지 않음을 타입 테스트 + 런타임 assert"
    on_violation: block_merge

# ─── 경계 ───────────────────────────────
out_of_scope:
  - "가격 문자열 포맷팅·통화 표기·i18n 문구 실제 텍스트 (DS4 소관. 본 모듈은 badgeKey·reasonKey 키만 반환)"
  - "의료광고법 표기 규율 (D4 규칙을 C4·C7 이 집행. 본 모듈은 축·카테고리별 분기를 두지 않는다)"
  - "가격 파싱·정규화 (C4) · 충돌 탐지 및 기록 (C4/C7)"
  - "confidence 산출 (C4)"
  - "정렬·필터·페이지네이션 (W2)"
  - "DB 조회 (F5)"

rollback: >
  `git revert <merge-sha>`. 순수 함수 패키지이며 DB·배포 상태를 변경하지 않는다.
  소비 태스크(W2·W3·W4·W5)가 이미 머지된 뒤 되돌려야 하는 경우, revert 는 컴파일 파손을 일으키므로
  대신 constants.ts·판정 함수만 이전 버전으로 되돌리는 패치 PR 을 낸다(패키지 삭제 금지).

done_when:
  - "`pnpm test:price-state && pnpm test:price-state-holdout && pnpm smoke:price-state` 전부 exit 0"
  - "위반 픽스처 4종(confidence null 보정 / 임계 중복 선언 / 비-confirmed showNumeric=true / conflicted 대표값 반환)이 각각 대응 검사를 실패시킴을 확인"
  - "REQ-5 고신뢰 40건이 전건 confirmed + showNumeric=true (전량 '미공개' 렌더 구현 차단 확인)"
  - "홀드아웃 40건이 개발 중 열람되지 않았음을 holdout.lock sha256 과 커밋 이력으로 확인"
  - "결정표 문서(24행)가 packages/price-state/README 에 존재하고 테스트와 1:1 대응"
  - "W2·W3·W4·W5 계약 저자에게 import 경로 `@glowmate/price-state` 와 4상태 계약이 전달됨 (각 계약의 touches 에서 apps/web/src/lib/price-state.ts 제거 상신)"
```
