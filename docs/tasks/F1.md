# F1 — 리포 스캐폴딩 · 모노레포 · CI

> 계약 규격: [02-task-contract-spec.md](../02-task-contract-spec.md) §1
> 개정: 2026-08-03 (F1 게이트 감사 `docs/audit/f1-gate.md` — blocking 10건 반영)
> 개정: 2026-08-03 (4차 감사 `docs/audit/f1-gate2.md` — NO-GO 2건 B-3b·B-8 해소)
> **분할:** 계약 거버넌스 정본(ids.yaml · docs/gates/** · dag-check)은 [F1b-CONTRACT-GOVERNANCE](./F1b.md) 로 분리했다(승인됨).

```yaml
# ─── 식별 ───────────────────────────────
id:            F1-REPO-SCAFFOLD
dag_id:        F1
title:         리포 스캐폴딩 · 모노레포 · CI (경계 집행 기반)
workstream:    foundation
owner_agent:   dev-platform

# ─── 존재 이유 ──────────────────────────
traces_to:     [H3, G1, G3]
why:           "W1·W4 의 대량 정적 생성(H3)이 성립하려면 렌더링 기본값이 처음부터 정적이어야 하고,
                G1·G3 증거 검증기(validate_d*.py)를 실제로 돌리는 discovery job 이 없으면 이후 모든 계약의
                on_violation:block_merge 는 집행 주체가 없는 선언에 그친다."

# ─── DAG ────────────────────────────────
depends_on:    []
blocks:        [D1a-PROTOCOL, D1b-FIELDWORK, D2-SEO-SERP-FEASIBILITY, D3-SOURCE-DUE-DILIGENCE, D4-MEDICAL-AD-GUARDRAIL, DS1-TOKEN-LAYERS, DS3-BASE-WIRING, DS6-A11Y-GATE, F1b-CONTRACT-GOVERNANCE, F2a-CORE-SCHEMA, F4-NEED-TAG-ONTOLOGY, F5-API-LAYER, O1-METRICS-DASHBOARD, O3-DEPLOY-MONITORING, W1-SEO-FOUNDATION]
parallel_with:  [D1a-PROTOCOL, D2-SEO-SERP-FEASIBILITY, D3-SOURCE-DUE-DILIGENCE, D4-MEDICAL-AD-GUARDRAIL]
gate:          null                  # Phase 1은 게이트와 무관하게 선행 가능

# ─── 산출물 ─────────────────────────────
deliverable:
  pr_count:    1
  touches:
    - package.json
    - pnpm-workspace.yaml
    - pnpm-lock.yaml                   # REQ-1(--frozen-lockfile)의 필수 생성물
    - README.md                        # done_when 의 필수 생성물
    - .nvmrc
    - .python-version
    - .gitignore
    - .env.example
    - eslint.config.mjs                # 루트 플랫 설정. 없으면 `pnpm lint` 자체가 성립하지 않는다
    - apps/web/**
    - packages/api/package.json        # 빈 스캐폴드 패키지 (구현은 F5)
    - packages/api/src/index.ts
    - packages/api/tsconfig.json       # REQ-2 의 세 플래그를 상속하는 수단. 없으면 REQ-2 가 충족 불가능하다
    - packages/db/package.json
    - packages/db/src/client.ts        # 커넥션 팩토리 골격만. 스키마·마이그레이션은 F2a/F2b/F2c
    - packages/db/tsconfig.json        # REQ-2 의 세 플래그를 상속하는 수단
    - packages/config/**               # tsconfig·lint 프리셋 + dependency-classes.json + data-access-names.json + db-driver-exceptions.json
    - services/crawler/**
    - .dependency-cruiser.cjs
    - .github/workflows/ci.yml
    - .github/ci-budget.json           # job별 timeout 예산 정본
    - .github/CODEOWNERS
    - .github/pr-task                  # 이 PR 이 구현하는 계약 ID 1줄 (FORBID-4 판정 원천)
    - .github/ci-fixtures/**           # 위반 픽스처 8종 (유형별 독립 트리)
    - tools/ci-meta/**                 # REQ-5·6·7 검사기
    - tools/dep-graph/**               # REQ-3 검사기
    - tools/path-guard/**              # FORBID-4 검사기
  artifacts:
    - "pnpm workspace 모노레포 (apps/web, packages/api, packages/db, packages/config)"
    - "services/crawler — Python 3.12 + Playwright 독립 툴체인"
    - "CI 8 job (typecheck / lint / boundary / test / python / secret-scan / discovery / path-guard) + 애그리게이터 ci-required"
    - "의존 도달성 + 의존 분류 이중 경계 검사 (dependency-classes.json · data-access-names.json · db-driver-exceptions.json)"
    - "위반 픽스처 8종 (유형별 독립 트리) + 귀속·신선도 검사"
    - "CODEOWNERS 7경로 + Code Owner 리뷰 필수화"

# ─── 요구사항 ───────────────────────────
requirements:
  - id: REQ-1
    statement: >
      pnpm workspace 가 apps/web · packages/api · packages/db · packages/config 4개를 반드시 포함하며
      (추가 패키지 허용), 클린 체크아웃에서 `pnpm install --frozen-lockfile && pnpm -r build` 가 exit 0 이다.
    acceptance: >
      CI job `test` — `pnpm list -r --depth -1 --json` 결과가 위 4개 이름을 전부 포함(집합 포함 검사, 개수 상한 없음)하고
      build exit code 0. 개수 동등 비교(=== 4)를 사용하면 실패로 간주한다.

  - id: REQ-2
    statement: >
      전 TS 패키지의 tsconfig 가 strict=true · noUncheckedIndexedAccess=true · noImplicitOverride=true 를
      상속하고 `pnpm typecheck` 가 오류 0건으로 종료한다.
    acceptance: "CI job `typecheck` — `tsc --noEmit` exit 0 + packages/config/tsconfig.base.json 의 세 플래그 값 assert 테스트 통과."

  - id: REQ-3
    statement: >
      DB 접근 보유 패키지는 packages/api 와 packages/db 둘뿐이며, 그 외 모든 워크스페이스 패키지
      (apps/web · packages/ui · 이후 신설분)에 대해 (a) 의존 그래프상 packages/db 및 Postgres 와이어
      드라이버 노드가 도달 불가능하고, (b) package.json 의 dependencies·devDependencies 전 항목이
      packages/config/dependency-classes.json 에 `data-access` 또는 `other` 로 분류되어 있으며
      `data-access` 로 분류된 항목이 0건이다.
    acceptance: >
      CI job `boundary` — `pnpm test:dep-graph` 가 (a) lockfile 도달성 판정과 (b) 분류 대조를 함께 수행하고
      **미분류 의존이 1건이라도 있으면 exit 1**(분류 강제 = 허용목록). 예외는
      packages/config/db-driver-exceptions.json 등재분(초기값 빈 배열)뿐이다.
      **분류 등재는 하류 계약에 열려 있다** — `dependency-classes.json` 은 FORBID-4 의 전 계약 공통 허용
      경로이며, 하류가 의존을 추가할 때 자기 PR 안에서 분류를 등재하는 것이 정상 경로다. 등재 diff 는
      (i) `other` 신규 추가는 승인 없이 허용, (ii) `data-access` 로 추가하거나 **기존 항목의 분류를 변경**하는
      diff 는 packages/config CODEOWNERS 승인 리뷰(승인자 ≠ PR 작성자) 없이는 exit 1,
      (iii) `packages/config/data-access-names.json` 의 알려진 데이터 접근 패키지 이름·글롭
      (`prisma` · `@prisma/*` · `@supabase/*` · `drizzle-orm` · `knex` · `typeorm` · `mongoose` · `pg` ·
      `postgres` · `mysql2` · `@vercel/postgres` 및 이후 추가분)에 매칭되는 이름을 `other` 로 분류하는
      diff 는 (ii)와 동일하게 승인을 요구한다. (iii)이 없으면 `other` 자유 등재가 (b) 전체를 무력화한다.
      (ii)(iii)의 승인 요구는 `single_maintainer` 상태(REQ-7 acceptance)에서 (c-3) 독립 PR 요구로 치환된다.
      (i) `other` 신규 추가는 어느 상태에서도 승인 없이 허용된다 — 이것이 DS1·DS3 의 합법 경로다.
      픽스처 4종 — ①`@vercel/postgres`(드라이버 도달) ②`@prisma/client`(도달하지 않으나 data-access 분류)
      ③미분류 신규 의존 은 각각 exit ≠ 0, ④**순수 UI 의존 1건(예: `@radix-ui/react-dialog`)을 `other` 로
      등재한 트리는 exit 0** — ④가 red 이면 DS1·DS3 에 합법 경로가 없다는 뜻이므로 실패로 간주한다(원칙 2.5).
      모듈명 부인목록만으로 구현하면 실패로 간주한다.

  - id: REQ-4
    statement: >
      services/crawler 는 pnpm workspace 에 포함되지 않고 python job 에서 ruff + pytest 로 실행되며,
      최소 1개 테스트가 "Playwright 브라우저 기동 후 로컬 고정 HTML 에서 지정 셀렉터 텍스트 1건 추출"을 assert 한다.
    acceptance: >
      CI job `python` — pytest 수집 테스트 수 ≥ 1 이고 위 assert 를 포함한 테스트가 통과.
      python 미설치 러너에서 `pnpm -r build` 가 exit 0 임을 별도 job step 으로 확인.

  - id: REQ-5
    statement: >
      CI 워크플로가 8개 job(typecheck, lint, boundary, test, python, secret-scan, discovery, path-guard)을
      PR 이벤트에서 실행하고, `.github/ci-fixtures/<job>/` 에 담긴 **위반 1종만 포함하는 독립 픽스처 트리 8개**
      각각에 대해 **대응 job 만 red 이고 나머지 7개는 green** 이며, 그 red 의 실패 사유가 해당 검사 규칙 ID
      또는 계약이 지정한 에러 문자열과 일치한다(귀속 검증).
    acceptance: >
      CI job `lint` 이 실행하는 `pnpm test:ci-meta` — (1) 워크플로 파싱으로 8개 job 이름이 전부 존재
      (집합 포함 검사, 개수 동등 비교 사용 시 실패), (2) 8개 픽스처 트리를 **현재 PR 의 워크플로 정의로
      재실행한 런**만 유효로 인정(워크플로 파일 sha 일치 = 신선도 검사. 과거 red 런 재사용 금지),
      (3) 각 런에서 대응 job red · 나머지 7개 green · 실패 로그가 규칙 ID 매칭임을 assert.
      하나라도 어긋나면 exit 1. 픽스처 8종은 ①`@vercel/postgres` 의존(boundary) ②사유 없는 eslint-disable(lint)
      ③force-dynamic 페이지 라우트(test) ④타입 오류(typecheck) ⑤가짜 자격증명(secret-scan)
      ⑥pytest 실패(python) ⑦항상 exit 0 인 스텁 validate 스크립트(discovery) ⑧touches 밖 경로 변경(path-guard)이며,
      각 트리는 lockfile 정합을 유지해야 한다 — install 실패로 전 job 이 red 가 되면 귀속 검증이 실패한다.

  - id: REQ-6
    statement: >
      각 job 의 timeout-minutes 가 `.github/ci-budget.json` 값과 정확히 일치하고 어떤 job 도 8분을 초과하지 않으며,
      8개 검사 job 은 `needs` 없이 병렬 실행되고, 예외로 애그리게이터 job `ci-required` 1개만
      `needs: [8개 job 전체]` 를 선언해 브랜치 보호의 **유일한 필수 체크**로 등록된다.
      **`ci-required` 는 needs 중 하나라도 결과가 `success` 가 아니면(failure · cancelled · skipped 포함)
      반드시 자신도 실패한다** — `needs` 만 선언한 구현은 선행 job 실패 시 자신이 실행되지 않고 `skipped` 로
      끝나며, 브랜치 보호는 skipped 필수 체크를 통과로 취급한다. 따라서 `if:` 로 실행을 보장하고
      `needs.*.result` 를 명시 평가해 non-success 가 1건이라도 있으면 exit 1 하는 스텝을 반드시 포함한다.
    acceptance: >
      `pnpm test:ci-meta` — timeout 불일치·8분 초과·검사 job 의 `needs` 선언 시 exit 1.
      GitHub API 조회로 (a) 브랜치 보호 required status checks 집합 == {`ci-required`},
      (b) `ci-required.needs` ⊇ ci.yml 에 정의된 전 검사 job 집합 임을 assert — 하류가 job 을 추가하고
      needs 에 넣지 않으면 실패한다. 하류 태스크는 자기 job 항목만 추가하며 기존 항목을 수정하지 않는다(FORBID-5).
      (c) **집행 검증** — REQ-5 픽스처 8종의 각 런에서 `ci-required` 의 conclusion 을 조회해
      `success` 가 아님을 assert 한다. 8종 중 1건이라도 `ci-required` 가 `success` 또는 `skipped` 로
      통과 처리되면 exit 1. 등록 상태(a)(b)만 검사하면 순진한 `needs` 구현이 계약을 100% 준수한 채
      전 job red 에서 머지를 열어주므로, 이 (c)가 없으면 36개 하류 계약의 `on_violation: block_merge` 가
      전부 자문으로 전락한다.

  - id: REQ-7
    statement: >
      .github/CODEOWNERS 가 packages/db · packages/api · packages/price-state · packages/config ·
      .github/** · docs/registry/** · tools/** 7개 경로에 소유자를 등록하고, 브랜치 보호의
      "Require review from Code Owners" 가 활성화되어 있으며, 각 경로의 소유자 집합이
      구현 에이전트 계정(PR 작성자 계정)을 포함하지 않는다.
      **단 리포지토리의 write 권한 보유 계정이 1개뿐인 동안에는** 타인 승인이 물리적으로 불가능하므로
      아래 `single_maintainer` 대체 규약이 적용된다 — 이 상태는 만료일을 갖는 한시 상태이며,
      만료 후에는 원 요구가 그대로 되살아난다.
    acceptance: >
      `pnpm test:ci-meta` — (a) CODEOWNERS 파싱 결과가 7개 경로 패턴을 전부 포함하고 소유자 필드가 비어 있지 않음,
      (b) GitHub API 로 `require_code_owner_reviews == true`, (c) 소유자 집합 ∩ 구현 에이전트 계정 == ∅.
      하나라도 실패 시 경로명과 함께 exit 1.
      (c) 는 GitHub API 로 조회한 **push 권한 보유 collaborator 수가 2 이상일 때** 판정한다. 1명이면
      `single_maintainer` 상태로 간주하고 (c) 대신 아래 3건을 전부 assert 한다 —
      (c-1) `.github/CODEOWNERS` 헤더에 `single-maintainer-until: <YYYY-MM-DD>` 가 있고 **오늘 ≤ 그 날짜**
      (초과하면 exit 1. 즉 이 완화는 스스로 만료한다),
      (c-2) 그 날짜가 CODEOWNERS 최초 도입일로부터 **90일 이내**,
      (c-3) 승인 대체 조건 — `packages/config/db-driver-exceptions.json` ·
      `packages/config/dependency-classes.json`(승인 대상 diff 인 경우) · `.github/ci-budget.json`(기존 항목 상향)
      의 변경은 **그 파일 하나만 포함하는 독립 PR** 이어야 하며 다른 변경과 섞이면 exit 1.
      대체 규약은 "타인이 봤다"를 흉내내지 않는다 — 검증 가능한 것(독립 PR · 만료)만 요구하고,
      검증 불가능한 것(타인 승인)은 만료일까지 유예했음을 CI 로그에 `SINGLE_MAINTAINER` 로 명시 기록한다.

  - id: REQ-8
    statement: >
      apps/web 이 Static 또는 ISR 로 렌더되는 **페이지 라우트**를 2개 이상 포함하고, `next build` 라우트 요약에
      해당 라우트가 Static/ISR 로 출력된다.
    acceptance: "CI job `test` — `next build` 요약 파싱 결과 Static·ISR 페이지 라우트 수 ≥ 2 가 아니면 exit 1 (라우트 0개 자동 통과 방지)."

# ─── 조건부 금지사항 ────────────────────
forbid:
  - id: FORBID-1
    when: >
      packages/api·packages/db 외의 워크스페이스 패키지에서 (a) 의존 그래프상 packages/db 또는 Postgres
      와이어 드라이버 노드가 도달 가능해지거나, (b) `data-access` 로 분류된 의존이 package.json 에
      등재되거나, (c) db-driver-exceptions.json 에 (사유 · 만료일 ≤ 90일 · 승인자) 3필드 없이 항목이 추가되는 경우
    must_not: "소비자 패키지에 DB 접근 경로를 도입하거나, 예외 목록을 근거 없이 늘려 경계를 해제하는 것"
    because: >
      모듈명 열거로 막으면 `@vercel/postgres` 한 줄로, 도달성만 보면 `@prisma/client`·`@supabase/supabase-js`
      로 뚫린다. 웹 라우트가 DB에 직접 붙는 순간 F5 의 public_venue 경유 강제가 무력화되어 C7 이 비공개로
      돌린 업체가 상세 API 로 계속 나가고, SSR 컴포넌트마다 커넥션이 생성돼 Vercel 함수에서 풀이 고갈된다.
      예외 파일에 게이트가 없으면 이 경계 전체가 하류 PR 한 줄로 해제된다.
    detect: >
      CI job `boundary` — REQ-3 의 도달성 + 분류 이중 검사 및 예외 항목 3필드 검사.
      db-driver-exceptions.json diff 는 packages/config CODEOWNERS 승인 리뷰(승인자 ≠ PR 작성자) 없이는 실패.
      `single_maintainer` 상태(REQ-7 acceptance)에서는 승인 리뷰 대신 (c-3) 독립 PR 요구가 적용된다 —
      완화가 아니라 검증 가능한 것으로의 치환이며, 3필드 검사와 만료일 검사는 그대로 유지된다.
      픽스처 ①②③ 이 boundary job 만 red 로 만드는지 확인하는 메타테스트(REQ-5).
    on_violation: block_merge

  - id: FORBID-2
    when: >
      이 PR 의 diff 가 CI 스텝의 종료 코드를 마스킹하거나 검사 대상을 축소하는 구성을 신규 도입하는 경우
      — `continue-on-error: true` · `|| true` · `|| :` · `set +e` · 스크립트 말미 `exit 0` · `if: always()` ·
      `--passWithNoTests` · `.skip`/`.only`/`@pytest.mark.skip` · 필수 체크에서 job 제외 ·
      사유 주석(`-- reason: <이슈 URL>`) 없는 `eslint-disable`·`depcruise-disable`
      (단 `.github/ci-fixtures/**` 와 검사기의 패턴 정의 파일은 위반 재현이 목적이므로 대상에서 제외하고,
      애그리게이터 `ci-required` job 의 `if:` 조건도 대상에서 제외한다 — 그 `if:` 는 실행을 건너뛰기 위한
      것이 아니라 선행 job 실패 시에도 반드시 실행돼 `needs.*.result` 를 평가하기 위한 것으로,
      REQ-6 의 실패 전파 요구를 만족시키는 유일한 수단이다. 제외 범위는 job 이름이 `ci-required` 인
      단 하나의 job 에 한정하며, 검사 job 8개의 `if:`·`continue-on-error` 는 그대로 대상이다)
    must_not: "규칙을 무력화하는 구성을 도입하는 것 (사유 주석 + 해당 경로 CODEOWNERS 승인 리뷰가 있는 disable 은 허용)"
    because: >
      green PR 에서 미리 심어둔 `|| true` 나 `describe.skip` 은 다음 PR 부터 검사를 영구 무력화한다.
      F1 의 CI 는 이후 40여 계약의 탐지 기반이므로, 여기서 우회 관용구가 한 번 허용되면 개발 에이전트가
      이를 학습해 계약 체계 전체가 장식이 된다. 판정 기준은 열거가 아니라 "종료 코드 마스킹 또는 검사 대상
      축소"라는 성질이며, 위 목록은 그 예시다.
    detect: >
      CI job `lint` — 위 성질에 해당하는 패턴 사전(계약 예시 전건 포함)으로 diff 스캔,
      `--report-unused-disable-directives` 병행, 테스트 수집 건수가 base 커밋 대비 감소하면 실패.
      검사 실효성은 REQ-5 픽스처 ②로 검증된다.
    on_violation: block_merge

  - id: FORBID-3
    when: >
      apps/web 의 **페이지 라우트**(`apps/web/src/app/**/page.tsx` 와 그 세그먼트 레이아웃)에
      `dynamic='force-dynamic'` · `revalidate=0` · `unstable_noStore` · `fetchCache='force-no-store'` ·
      `cache:'no-store'` 중 하나가 선언되거나, `next build` 요약의 Static/ISR 페이지 라우트 수가 2 미만인 경우
      (Route Handler `apps/web/src/app/api/**` · Server Action · middleware 는 정의상 동적이므로 대상이 아니다)
    must_not: "페이지 라우트의 렌더링 기본값을 동적으로 전환하거나, 정적 라우트가 없는 상태로 검사를 공허하게 만드는 것"
    because: >
      채널 1순위가 SEO 이고 W4 조합 랜딩은 대량 정적 생성이 전제다. 페이지 기본값이 동적이면 수천 개 랜딩이
      색인되지 않는다. 반대로 Route Handler 까지 정적을 요구하면 F5·W6·W8 에서 규칙이 반드시 깨지고,
      깨지는 순간의 최단 경로가 `|| true`(FORBID-2 위반)다. 그래서 대상을 페이지 라우트로 한정한다.
    detect: >
      CI job `test` — 1차 수단: `next build` 요약에서 **페이지 라우트만** 필터해 동적 항목 0건 및 Static/ISR ≥ 2(REQ-8).
      2차 수단: 페이지 라우트 파일에 한정한 5개 토큰 grep. 둘 중 하나라도 실패하면 job 실패.
    on_violation: block_merge

  - id: FORBID-4
    when: >
      PR 의 변경 파일 집합이 `.github/pr-task` 에 기재된 계약 ID 의 `docs/tasks/<ID>.md` touches 글롭 밖
      경로를 포함하는 경우 (`.github/pr-task` 자체 · `pnpm-lock.yaml` ·
      `packages/config/dependency-classes.json` 은 전 계약 공통 허용 — 마지막 항목은 REQ-3 (b)가 전 패키지에
      분류를 강제하므로 의존을 추가하는 모든 하류 계약이 편집해야 하는 파일이다. 이 허용이 없으면
      boundary red 와 path-guard red 사이에 합법 경로가 0개가 된다)
    must_not: "타 태스크 소유 경로를 선행 작성·수정하는 것"
    because: >
      F1 에서 테이블이나 토큰이 먼저 생기면 F2a·DS1 이 첫 작업부터 파괴적 마이그레이션·중복 정의로 시작해야 하고,
      F1 롤백 시 되돌림 단위가 엉켜 revert 가 불가능해진다. 판정 원천을 못박지 않으면 F1 이 자기 touches 를
      하드코딩하게 되고, 그 순간 모든 하류 PR 이 이 검사에서 실패한다.
    detect: >
      CI job `path-guard` — `tools/path-guard` 가 `.github/pr-task` 의 계약 ID 로 `docs/tasks/<ID>.md` 의
      touches 블록을 직접 파싱(F1b 의 docs/tasks.json 에 의존하지 않는 자립 경로)해
      `git diff --name-only origin/main` 과 대조, 밖의 경로가 1건이라도 있으면 exit 1.
      `.github/pr-task` 부재이거나 미등재 계약 ID 인 경우도 exit 1.
      job 이름은 `path-guard` 로 고정한다 — C2·C3 계약이 이 이름을 인용한다.
    on_violation: block_merge

  - id: FORBID-5
    when: >
      base 브랜치에 이미 존재하는 `.github/ci-budget.json` 의 기존 job 항목 값을 상향하거나,
      job당 상한 8분을 초과하는 값을 도입하는 경우 (신규 job 항목 추가는 해당하지 않는다)
    must_not: "timeout 상향으로 CI 실패를 해결하는 것 (테스트 분할·캐시·job 분리로 해결)"
    because: >
      상한을 올리는 것이 언제나 최단 경로다. job 이 누적되며 예산이 계속 오르면 PR 피드백이 40분이 되고,
      그 시점에 개발 에이전트가 택하는 다음 행동은 job 을 지우는 것이다.
    detect: >
      CI job `lint` — ci-budget.json diff 가 기존 항목 상향이면 `.github` CODEOWNERS 승인 리뷰
      (승인자 ≠ PR 작성자, GitHub API 조회) 없이는 exit 1. base 에 파일이 없는 최초 도입 커밋은 검사 대상에서 제외.
      `single_maintainer` 상태에서는 승인 리뷰 대신 REQ-7 (c-3) 독립 PR 요구가 적용된다.
    on_violation: block_merge

  - id: FORBID-6
    when: >
      (a) **존재 요구** — origin/main 에 머지된 Discovery 계약이 선언한 `scripts/discovery/validate_d*.py` 가
      리포지토리에 없는 경우, 또는 (b) **비스텁 요구** — 머지 여부와 무관하게 리포지토리에 존재하는
      `scripts/discovery/validate_d*.py` 중 하나라도 인자·입력과 무관하게 항상 exit 0 을 반환하는 경우
    must_not: "대상 부재 또는 스텁 상태를 discovery job 성공으로 처리"
    because: >
      없는 검증기를 도는 job 은 영구 초록이고, 그 위에서 Discovery 계약의 FORBID 22개가 집행된 것처럼
      보인 채 G1·G3 판정이 내려진다. (a)만 두면 D 계약이 머지될 때까지 job 이 아무것도 검사하지 않고,
      (b)까지 미머지 계약에 걸면 F1 자신의 PR 이 차단된다. 두 갈래로 나누어야 둘 다 성립한다.
    detect: >
      CI job `discovery` — (a) 머지된 계약 목록에서 요구 스크립트를 산출해 부재 시 실패,
      (b) 존재하는 전 validate 스크립트에 결손 입력을 주입해 non-zero 를 내는지 확인하고 항상 exit 0 이면 실패.
      REQ-5 픽스처 ⑦(스텁 스크립트)은 (b)로 판정되어 discovery job 만 red 가 된다.
    on_violation: block_merge

# ─── 경계 ───────────────────────────────
out_of_scope:
  - "docs/registry/ids.yaml · docs/metrics/registry.yaml · docs/gates/*.md · tools/dag-check/** (F1b-CONTRACT-GOVERNANCE)"
  - "DB 테이블·컬럼·마이그레이션 정의 (F2a / F2b / F2c) — packages/db 의 드라이버 의존 추가는 F2a 소관"
  - "packages/api 의 조회 함수·zod 계약 구현 (F5) — 본 태스크는 빈 스캐폴드 패키지만 생성"
  - "디자인 토큰·UI 컴포넌트 (DS1~DS7)"
  - "크롤러 어댑터 로직 (C1~C3) — services/crawler 는 REQ-4 스모크 테스트 1개까지"
  - "배포 환경·모니터링·알림 (O3)"
  - "validate_d*.py 스크립트 작성 (각 Discovery 계약 소관) — F1 은 실행 job 과 스텁 판정만 제공"
  - "03-task-dag.md 레지스트리 갱신 (문서 PR 소관. F1b 착수 전 필수)"
  - "인증·세션·로그인"

rollback: >
  신규 파일 생성만 포함하므로 `git revert <merge-sha>` 로 전량 원복된다. 외부 상태 변경은
  (1) GitHub Actions 워크플로 등록, (2) 브랜치 보호 필수 체크 `ci-required` 1건 + Code Owner 리뷰 필수화,
  (3) CODEOWNERS 7경로 규칙, (4) 픽스처 실행용 브랜치이며 revert 시 (2)를 해제하고 (4)를 삭제한다.
  DB·배포 대상 없음.

done_when:
  - "클린 체크아웃에서 `pnpm install --frozen-lockfile && pnpm -r build && pnpm typecheck && pnpm lint && pnpm test && pnpm test:dep-graph && pnpm test:ci-meta` 전부 exit 0"
  - "픽스처 8종 각각의 실제 Actions 런에서 **대응 job 만 red · 나머지 7개 green** 이고 실패 사유가 규칙 ID 로 귀속됨을 URL 로 첨부"
  - "각 픽스처 트리가 lockfile 정합을 유지해 install 단계에서 실패하지 않음을 확인 (결합 실패로 전 job 이 red 가 되는 상태가 아님)"
  - "REQ-3 픽스처 4종 중 ①`@vercel/postgres` ②`@prisma/client` ③미분류 의존은 boundary job 만 실패시키고, ④`other` 로 등재한 순수 UI 의존은 boundary green 임을 확인 (④가 red 면 DS1·DS3 합법 경로 부재)"
  - "브랜치 보호 필수 체크가 `ci-required` 1건이고 `ci-required.needs` 가 8개 job 을 전부 포함하며, Code Owner 리뷰 필수화가 활성 상태임을 API 응답으로 첨부"
  - "픽스처 8종 각 런에서 `ci-required` 의 conclusion 이 `success` 가 아님을 API 응답으로 첨부 (skipped 통과 semantics 로 머지가 열리지 않음의 실증)"
  - "CODEOWNERS 7경로 소유자가 구현 에이전트 계정과 다른 주체임을 확인"
  - "`next build` 요약에 Static/ISR 페이지 라우트가 2개 이상 출력되고, Route Handler 가 동적이어도 test job 이 green 임을 확인"
  - "README 에 워크스페이스 경계 다이어그램, 8개 job + ci-required 의 목적 1줄 설명, ci-budget 표, 픽스처 8종 표 포함"
```

---

## 게이트 감사 blocking 10건 반영 위치

| # | 지적 | 반영 |
|---|---|---|
| **B-1** | 픽스처 결합 → 검사 로직 0줄로도 통과 | **REQ-5 전면 교체** — `.github/ci-fixtures/<job>/` 유형별 독립 트리 8개 · **대응 job만 red / 나머지 7 green** · 실패 사유 **귀속**(규칙 ID 매칭) · **신선도**(워크플로 sha 일치) · 각 트리 lockfile 정합 의무 |
| **B-2** | discovery 영구 초록 | **FORBID-6 when 을 (a)존재 / (b)비스텁 두 갈래로 분리.** (b)는 머지 여부와 무관한 전 스크립트 대상 → 픽스처 ⑦이 실제로 discovery 를 red 로 만든다 |
| **B-3** | 제외 집합이 아키텍처와 불일치 → F2a 착수 시 boundary red | **REQ-3·FORBID-1 의 제외 집합을 `{packages/api, packages/db}`** 로 정정. F5·F2a 와 3자 정합(별도 수정 완료) |
| **B-3b** | Prisma·Supabase 우회 | REQ-3 (b) **의존 분류 강제** — 전 의존이 `dependency-classes.json` 에 분류되어야 하고 **미분류 1건이면 exit 1**(허용목록화), `data-access` 분류 0건 요구 |
| **B-3b′** (4차) | 허용목록 기대값 파일이 F1 소유라 DS1·DS3 에 합법 경로 0개 | FORBID-4 공통 허용에 `dependency-classes.json` 추가(등재 경로 개방) + REQ-3 acceptance 에 **`other` 무승인 / `data-access`·분류변경 승인 필요 / `data-access-names.json` 매칭명의 `other` 위장도 승인 필요**(iii) + **픽스처 ④(UI 의존 `other` 등재 → green)** 로 합법 경로를 검사 대상화(원칙 2.5) |
| **B-4** | 예외 파일 사후 추가 무방비 | FORBID-1 when (c) — **사유·만료일 ≤90일·승인자 3필드** 없는 등재 금지 + REQ-7 CODEOWNERS 에 `packages/config` 추가 |
| **B-5** | FORBID-4 판정 수단 부재 | **`.github/pr-task`(계약 ID 1줄)** + `docs/tasks/<ID>.md` touches 직접 파싱(F1b 산출물 비의존 자립 경로). job 명을 **`path-guard`** 로 고정(C2·C3 인용과 통일) |
| **B-6** | 자기 PR 차단 3회차 | touches 에 `eslint.config.mjs` · `tools/ci-meta/**` · `tools/dep-graph/**` · `tools/path-guard/**` · `.github/ci-fixtures/**` · `.github/pr-task` 추가. FORBID-2 when 에 **픽스처·패턴 정의 파일 제외**를 규칙 안으로 |
| **B-7** | FORBID-3 detect 가 무조건 금지 | when·detect 를 **페이지 라우트로 한정**하고 Route Handler·Server Action·middleware 예외를 규칙 안에 명시 |
| **B-8** | 필수 체크 등록 메커니즘 부재 | **REQ-6 에 애그리게이터 `ci-required` 예외 신설** — 유일한 필수 체크로 등록 + `needs ⊇ 전 job` 을 API 로 검증(하류 job 이 자동 편입) |
| **B-8′** (4차) | 등록만 검사하고 **실패 전파**를 아무도 안 봄 → skipped=통과로 전 job red 에서 머지 열림 | REQ-6 statement 에 **non-success 전파 의무**(`if:` + `needs.*.result` 명시 평가) + acceptance (c) **픽스처 8종 각 런의 `ci-required` conclusion ≠ success** assert + FORBID-2 when 제외절에 **`ci-required` job 의 `if:` 한정 제외**(P7 5회차 예방) + done_when 1줄 |
| **B-9** | CODEOWNERS 집행 수단 없음 | REQ-7 acceptance 에 **Code Owner 리뷰 필수화 API 확인** + **소유자 ∩ 구현 에이전트 계정 = ∅** 추가 |
| **B-10** | `blocks` DS1 누락 | `DS1-TOKEN-LAYERS`·`DS6-A11Y-GATE` 추가 (검증기 통과 확인) |

**추가 반영(non-blocking):** N-1(FORBID-2 를 열거형 → "종료 코드 마스킹·검사 대상 축소" **성질 판정** + 테스트 스킵·수집 건수 감소 금지) · N-2(`test:ci-meta` 실행 job 을 `lint` 로 명시) · N-4(REQ-5 에 집합 포함 검사 문장) · N-5(`why` 에 H3 기여 경로를 "W1·W4 정적 생성 전제"로 구체화).
