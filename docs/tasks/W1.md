# W1 — SEO 기반 (라우팅 · 메타 · robots · sitemap · 렌더링 전략/무효화)

> 개정 2026-08-03 — `docs/audit/W1-O3-audit.md` W1 항목 및 `03-task-dag.md` 정정 #1·#2·#3·#5·#9 반영.
> 주요 변경: JSON-LD 소유권 W3 이관 · `is_public` → `visibility` ENUM · `revalidate` 상한 신설 · 무효화 경로 소유 · DB 경계 FORBID를 F1/F5로 이관 · 공용 메타 빌더 medical 가드레일 신설.

```yaml
# ─── 식별 ───────────────────────────────
id:            W1-SEO-FOUNDATION
title:         "SEO 기반 — URL 규약 · 메타/canonical · robots · sitemap · 렌더링 전략과 캐시 무효화 소유"
workstream:    web
owner_agent:   dev-web

# ─── 존재 이유 ──────────────────────────
traces_to:     [H3]
why:           "이것이 없으면 W2·W3·W4가 각자 URL 규칙·캐시 수명·색인 정책을 만들어, C7이 비공개로 돌린 데이터가 캐시로 계속 서빙되고 H3(롱테일 1페이지 진입)을 측정할 색인 단위 자체가 성립하지 않는다."

# ─── DAG ────────────────────────────────
depends_on:    [F1-REPO-SCAFFOLD, F2b-QUALITY-SCHEMA, F5-API-LAYER, DS3-BASE-WIRING, D4-MEDICAL-AD-GUARDRAIL]   # visibility ENUM 만 참조 (venue 기본 필드는 F5 응답 경유이므로 F2a 직접 의존 없음)
blocks:        [W2-DISCOVERY-LIST, W3-VENUE-DETAIL, W4-COMBO-LANDING, W5-EDITOR-REPORT, W7-CORRECTION-REQUEST, W9-OBSERVABILITY-EVENTS]
parallel_with: [C2-ADAPTER-NAVER, C3-ADAPTER-KAKAO, C4-PRICE-NORMALIZER, C5-ENTITY-RESOLUTION]
gate:          [G1, G2, G3]   # PRD §4 — Phase 3은 Phase 2 이후. Phase 3 전 계약이 동일 선언을 사용한다

# ─── 산출물 ─────────────────────────────
deliverable:
  pr_count:    1
  touches:
    - apps/web/src/lib/routes/**                     # buildPath 단일 소스 + 색인 라우트 매니페스트 도출기
    - apps/web/src/lib/seo/**                        # metadata · canonical 빌더 (JSON-LD 제외)
    - apps/web/src/lib/sitemap-sources/index.ts      # 제공자 자동 수집 규약 (개별 제공자는 W2·W3·W4·W5 소유)
    - apps/web/src/app/layout.tsx
    - apps/web/src/app/sitemap.ts
    - apps/web/src/app/robots.ts
    - apps/web/src/app/not-found.tsx
    - apps/web/src/app/api/internal/revalidate/route.ts
    - apps/web/tests/seo/**
    - apps/web/fixtures/seo/**
    - tools/ci/check-render-strategy.mjs
    - apps/web/package.json                          # scripts 필드 항목 추가만 (기존 스크립트 수정·삭제 금지)
  artifacts:
    - "buildPath() 경로 빌더 + 경로 파서 + 하드코딩 허용 allowlist(항목 상한 3)"
    - "색인 라우트 매니페스트 자동 도출기(app 디렉터리 스캔)"
    - "generateMetadata 빌더(title/description/canonical/openGraph) + D4 사전 검사 훅"
    - "robots 환경별 정책 · sitemap index 분할 + 제공자 자동 수집"
    - "렌더링 전략 CI 체커(revalidate 상한 포함)"
    - "서명 검증 캐시 무효화 엔드포인트 POST /api/internal/revalidate"

# ─── 요구사항 ───────────────────────────
requirements:
  - id: REQ-1
    statement: "apps/web/src 전체에서 내부 경로 문자열 하드코딩(`href=\"/`, `url: \"/`) 검출 건수가 0이며, 예외 allowlist(apps/web/src/lib/routes/allowlist.json)의 항목 수는 3 이하다."
    acceptance: "`pnpm --filter @glowmate/web ci:check-url-source` — 검출 1건 이상 또는 allowlist 항목 4개 이상이면 종료 코드 1."
  - id: REQ-2
    statement: "색인 라우트 매니페스트는 app 디렉터리의 page.tsx 스캔 결과에서 자동 도출되며, 수기 목록과의 차집합이 0이다(검사 대상 축소 불가)."
    acceptance: "`pnpm --filter @glowmate/web test:route-manifest` — 스캔 결과와 매니페스트의 대칭차집합이 1건 이상이면 실패."
  - id: REQ-3
    statement: "색인 라우트 전수(medical_wellness 픽스처 포함)에 대해 generateMetadata 가 title·description·alternates.canonical·openGraph 4개 필드를 반환하고, title 이 빈 문자열인 라우트가 0건이다."
    acceptance: "`pnpm --filter @glowmate/web test:seo-metadata` — 매니페스트 전수 순회에서 4필드 중 undefined 또는 빈 title 이 1건이라도 있으면 실패. (FORBID-4의 짝 — medical 레코드를 통째로 숨기면 이 REQ가 실패한다)"
  - id: REQ-4
    statement: "canonical URL 은 (a) 환경변수 SITE_ORIGIN 기반 절대 URL, (b) 소문자 경로, (c) 쿼리스트링 제거, (d) trailing slash 없음 4개 규칙을 만족한다."
    acceptance: "`pnpm --filter @glowmate/web test:seo-canonical` — 입력 40개 케이스가 전부 기대 문자열과 정확 일치, SITE_ORIGIN 미설정 시 빌드 실패."
  - id: REQ-5
    statement: "robots.txt 는 VERCEL_ENV=production 에서 /api 와 쿼리 포함 경로를 Disallow 하고, production 이 아닌 전 환경에서 `Disallow: /` 를 출력한다."
    acceptance: "`pnpm --filter @glowmate/web test:robots` — VERCEL_ENV 3종(production/preview/development) 출력 스냅샷 일치."
  - id: REQ-6
    statement: "색인 대상 라우트 파일 전수가 `export const revalidate = <n>` (n ≤ 3600) 또는 `export const dynamic = 'force-static'` 중 하나를 선언하며, n > 3600 선언 파일 수는 0이다."
    acceptance: "`node tools/ci/check-render-strategy.mjs` — 미선언·상한초과·force-dynamic 파일이 1건이라도 있으면 종료 코드 1."
  - id: REQ-7
    statement: "POST /api/internal/revalidate 는 HMAC 서명이 유효한 요청에 대해 대상 slug 의 캐시를 무효화하고, 무효화 호출 후 60초 이내에 해당 URL 응답이 새 visibility 상태를 반영한다(서명 불일치 요청은 401)."
    acceptance: "`pnpm --filter @glowmate/web test:e2e:revalidate` — visibility 를 public→hidden_request 로 전이 후 무효화 호출 시 60초 내 404 확인, 서명 없는 요청 401 확인."
  - id: REQ-8
    statement: "C7 public_venue 스냅샷(스테이징 실데이터)으로 빌드했을 때 sitemap 에 포함된 URL 수가 스냅샷의 visibility='public' 업체 수 + 색인 대상 정적 라우트 수와 정확히 일치한다(누락 0건)."
    acceptance: "`pnpm --filter @glowmate/web smoke:sitemap --env=staging` — 개수 불일치 또는 누락 1건 이상이면 종료 코드 1. 모집단은 스냅샷 전량이며 표집·추출을 하지 않는다(R-6 비적용). (FORBID-2의 짝 — 전부 비공개 처리하면 이 REQ가 실패한다)"

# ─── 조건부 금지사항 ────────────────────
forbid:
  - id: FORBID-1
    when:         "REQ-2 매니페스트에 등재된 색인 대상 라우트 파일을 작성·수정할 때"
    must_not:     "`dynamic = 'force-dynamic'` 을 선언하거나 `revalidate` 를 3600 초과 값으로 선언하거나, 업체명·회당 단가·니즈 태그를 클라이언트 fetch/useEffect 로만 채우도록 구현"
    because:      "크롤러는 첫 HTML만 수집하므로 CSR 콘텐츠는 H3 진입을 0으로 만들고, revalidate 상한이 없으면 C7이 비공개로 돌린 업체가 정적 캐시로 무기한 서빙되어 G4가 빌드 시점에만 존재하는 게이트가 된다."
    detect:       "`tools/ci/check-render-strategy.mjs`(force-dynamic·상한 초과 파싱) + JS 비활성 fetch 로 얻은 W1 소유 스텁 라우트 HTML 에 픽스처 문자열이 존재하는지 검증. W2·W3·W4 라우트에 대한 동일 검사는 각 계약이 자기 라우트에서 수행한다."
    on_violation: block_merge
  - id: FORBID-2
    when:         "sitemap 제공자·라우트 핸들러·generateStaticParams·서버 컴포넌트·빌드 스크립트 중 어느 경로에서든 venue 레코드를 취득했고 그 레코드의 visibility 가 'public' 이 아닌 경우(hidden_quality·hidden_request·hidden_legal)"
    must_not:     "해당 URL 을 sitemap 항목으로 출력하거나 그 라우트에서 200 응답 본문을 렌더"
    because:      "C7 공개 판정과 W8 takedown 을 웹 레이어가 우회하는 경로다. hidden_legal·hidden_request 업체가 색인되면 삭제 요청을 이행하지 못한 상태가 검색결과에 수 주간 남아 법적 리스크가 실현된다."
    detect:       "통합테스트 — visibility 4개 값 픽스처 각각에 대해 sitemap 출력과 라우트 상태 코드를 검증(public만 200·등재, 나머지 3종은 0건·404)."
    on_violation: block_merge
  - id: FORBID-3
    when:         "apps/web/src/lib/seo/** 또는 apps/web/src/lib/routes/** 에서 canonical·openGraph.url·sitemap loc 의 오리진을 결정할 때"
    must_not:     "요청 헤더(host, x-forwarded-host, next/headers)에서 읽은 값을 오리진으로 사용"
    because:      "프리뷰 도메인(*.vercel.app)이 canonical 로 나가면 프로덕션 도메인과 중복 색인되어 링크 권한이 분산되고, 프리뷰 URL 이 검색결과에 노출된다."
    detect:       "ESLint no-restricted-imports — lib/seo/**·lib/routes/** 에서 `next/headers` import 금지 + REQ-4 테스트(SITE_ORIGIN 미설정 시 빌드 실패)."
    on_violation: block_merge
  - id: FORBID-4
    when:         "공용 generateMetadata 빌더가 category='medical_wellness' 인 레코드로 title·description·openGraph 문자열을 생성할 때"
    must_not:     "docs/discovery/D4/forbidden_lexicon.yaml 검사를 거치지 않은 문자열을 반환하거나, 시술명과 금액을 결합한 문구를 메타에 출력"
    because:      "메타 문자열은 검색 스니펫으로 사이트 밖에서 유통되어 회수할 수 없다. W2~W5는 본문에만 D4 검사를 걸어 두었으므로 공용 빌더가 무검사면 전 화면의 메타가 통째로 우회 경로가 된다."
    detect:       "단위테스트 — medical 픽스처 10건(금칙 표현 포함 5건)을 빌더에 입력해 금칙 매칭 0건 및 검사 훅 호출 여부를 검증."
    on_violation: block_merge
  - id: FORBID-5
    when:         "리소스(업체·리스트·조합·리포트)의 경로 형태를 추가하거나 변경할 때"
    must_not:     "lib/routes 밖에서 경로 문자열을 조립하거나, 동일 리소스에 대해 2개 이상의 경로 형태를 동시에 유효하게 유지하거나, allowlist 항목을 4개 이상으로 늘림"
    because:      "W4가 수백~수천 조합 페이지를 생성하는데 URL 규칙이 두 벌이 되면 canonical 불일치로 중복 페이지가 대량 색인되고, 정리하려면 전체 재빌드와 리다이렉트 맵 작성이 필요해진다."
    detect:       "REQ-1 스크립트(하드코딩 0건 + allowlist 상한 3) + 라우트 매니페스트 스냅샷 테스트(리소스당 경로 패턴 1개)."
    on_violation: block_merge
  - id: FORBID-6
    when:         "라우트 파라미터 zod 검증에 실패했거나 @glowmate/api 조회가 404 를 반환한 경우"
    must_not:     "상태 코드 200 과 함께 '결과 없음' 화면을 반환"
    because:      "W4가 만드는 대량 URL 에서 soft 404 가 발생하면 크롤 예산이 빈 페이지에 소진되고 사이트 단위 품질 평가가 하락해 정상 페이지 순위까지 동반 하락한다."
    detect:       "e2e — 미존재 slug 3건과 비공개 slug 3건의 응답 상태 코드가 전부 404 임을 검증."
    on_violation: block_merge

# ─── 경계 ───────────────────────────────
out_of_scope:
  - "LocalBusiness JSON-LD 빌더 — W3-VENUE-DETAIL 소유로 이관(상세 라우트에서만 사용)"
  - "리스트·상세·조합·리포트 라우트 구현과 각 sitemap 제공자 파일 (W2·W3·W4·W5)"
  - "웹→DB 직접 호출 차단 경계 — F1-REPO-SCAFFOLD REQ-3(boundary job)과 F5-API-LAYER 소유. W1은 규칙을 재정의하지 않는다"
  - "무효화 호출자 구현 — C7-QUALITY-GATE·W8-CORRECTION-PROCESSING 이 전이 시 호출한다. W1은 엔드포인트만 제공"
  - "계측 이벤트 (W9-OBSERVABILITY-EVENTS·W6-LEAD-TRACKING)"
  - "CI 워크플로 파일 수정 — 검사 스크립트는 apps/web package.json scripts 에 등록해 F1의 test·lint job 에서 실행한다"
  - "Search Console·네이버 서치어드바이저 등 외부 콘솔 설정 (O3)"
rollback:      "PR 1건 revert. 전부 신규 파일이며 기존 라우트를 대체하지 않는다. revert 시 무효화 엔드포인트가 사라지므로 C7·W8의 무효화 호출은 404를 받게 되고, 이 경우 운영은 배포 트리거(전체 재빌드)로 대체한다 — 롤백 시 이 사실을 O3 런북에 기록한다. 이미 색인된 URL 이 있으면 revert 배포 후 robots 전체 차단(비프로덕션 정책)으로 재크롤을 막고 잔존 색인은 Search Console 제거 요청으로 처리한다."
done_when:
  - "REQ-1~REQ-8 의 acceptance 명령이 전부 통과하고, 각 명령이 apps/web package.json scripts 에 등록되어 F1의 test·lint job 에서 실행됨"
  - "REQ-8 스테이징 스모크 실행 로그(스냅샷 업체 수·sitemap URL 수)가 PR 본문에 첨부됨"
  - "sitemap URL 45,000 초과 시 index 분할이 동작함을 60,000건 제공자 픽스처로 확인"
  - "FORBID-1~6 각각에 대해 의도적 위반 픽스처가 실패하는 것을 확인한 기록이 PR 본문에 첨부"
  - "W2·W3·W4·W5가 import 할 buildPath·라우트 매니페스트·sitemap 제공자 인터페이스가 export 되고 타입이 공개됨"
```
