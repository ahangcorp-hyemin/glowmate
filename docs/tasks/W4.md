# W4 — 조합 랜딩 생성기(지역×카테고리×니즈)

> 개정 2026-08-03 — `docs/audit/W1-O3-audit.md` W4 항목(공격 A-1~A-3) 및 `03-task-dag.md` 정정 #1·#2·#3·#5·#6·#9 반영.
> 주요 변경: 중복 검사 **표본 폐지 → 전수** · 근사 중복(결과집합 유사) canonical 통합 · **리스트 파생이 아닌 조합 고유 텍스트 하한 신설** · 빌드 후 `visibility` 변경 재검증 신설 · `depends_on` 에 C7·F4·W2·F5·F6 추가 · CI 워크플로 touches 제거(스크립트를 F1 test job 에 연결) · 생성 커버리지 하한 REQ 신설(원칙 2.5 · R-7).

```yaml
# ─── 식별 ───────────────────────────────
id:            W4-COMBO-LANDING
title:         "조합 랜딩 생성기 — 지역×카테고리×니즈 롱테일 페이지 정적 생성"
workstream:    web
owner_agent:   dev-web

# ─── 존재 이유 ──────────────────────────
traces_to:     [H3, S1]
why:           "채널 1순위인 SEO의 유입 표면 그 자체이며, 이것이 없으면 H3(롱테일 100개 1페이지 진입)을 검증할 페이지가 존재하지 않는다."

# ─── DAG ────────────────────────────────
depends_on:    [W1-SEO-FOUNDATION, W2-DISCOVERY-LIST, C4-PRICE-NORMALIZER, C6-TAG-ASSIGN, C7-QUALITY-GATE, F4-NEED-TAG-ONTOLOGY, F5-API-LAYER, F6-PRICE-STATE, D2-SEO-SERP-FEASIBILITY, D4-MEDICAL-AD-GUARDRAIL]
blocks:        []
parallel_with: [W6-LEAD-TRACKING]
gate:          [G1, G2, G3]   # 프로덕션 색인 활성화는 추가로 G4 통과 후 (done_when 참조)

# ─── 산출물 ─────────────────────────────
deliverable:
  pr_count:    1
  touches:
    - apps/web/src/app/(landing)/**                # 조합 랜딩 라우트 세그먼트
    - apps/web/src/lib/landing/**                  # 매니페스트 로더 · 자격 판정 · 통계 · 고유 텍스트 조립
    - apps/web/content/landing-keywords.json       # D2 enterable 파생 빌드 입력 매니페스트
    - apps/web/src/components/landing/**
    - apps/web/src/lib/sitemap-sources/landing.ts  # W1 규약에 따른 제공자 추가 (sitemap.ts 수정 금지)
    - tools/ci/check-landing-duplication.mjs
    - apps/web/tests/landing/**
    - apps/web/fixtures/landing/**
    - apps/web/package.json                        # scripts 항목 추가만
  artifacts:
    - "landing-keywords.json (D2 enterable 라벨 행 파생 + rule.lock 해시 기록) + 로더/zod 스키마"
    - "조합 자격 판정기(공개 업체 수 하한 · 결과집합 근사중복 통합)"
    - "generateStaticParams 정적 생성 + 조합 고유 텍스트 블록"
    - "confirmed 요금제 기반 가격 통계 계산기(표본 수 동시 노출)"
    - "전수 근사중복 검사 스크립트(simhash + LSH) · 생성/제외 리포트 JSON"

# ─── 요구사항 ───────────────────────────
requirements:
  - id: REQ-1
    statement: "landing-keywords.json 의 모든 행이 D2 산출물에서 enterable 로 라벨된 키워드 행에 존재하며, 파일에 기록된 rule.lock 해시가 docs/discovery/D2/rule.lock 값과 일치한다."
    acceptance: "`pnpm --filter @glowmate/web test:landing-provenance` — enterable 집합에 없는 행이 1건이라도 있거나 해시가 불일치하면 종료 코드 1."
  - id: REQ-2
    statement: "조합의 visibility='public' 업체 수가 5 미만이면 그 조합은 생성 목록에서 제외되고 제외 사유 코드가 리포트 JSON 에 1행 기록된다."
    acceptance: "`pnpm --filter @glowmate/web test:landing-threshold` — 공개 업체 4건/5건 경계 픽스처에서 생성 여부와 사유 코드 기록이 기대값과 불일치하면 실패."
  - id: REQ-3
    statement: "각 조합 페이지는 리스트(W2) 렌더 텍스트에서 파생되지 않는 조합 고유 문장을 5개 이상 포함하며, 그 문장들이 다른 조합 페이지와 5-gram 단위로 중복되는 비율이 0 이다."
    acceptance: "`pnpm --filter @glowmate/web test:landing-unique-text` — 고유 문장 5개 미만이거나 타 조합과 5-gram 중복이 1건이라도 발생하면 실패."
  - id: REQ-4
    statement: "생성된 조합 페이지 전수(표본 추출 없음)에 대해 본문 5-gram Jaccard 유사도가 0.8 을 초과하는 페이지 쌍이 0 이다."
    acceptance: "`node tools/ci/check-landing-duplication.mjs`(simhash+LSH 로 후보쌍 산출, 검사 대상은 생성물 전량) 가 종료 코드 0. 전수 검사이므로 표집·홀드아웃을 적용하지 않는다(R-6)."
  - id: REQ-5
    statement: "결과 업체 ID 집합의 Jaccard 유사도가 0.9 이상인 조합 그룹에서는 1개만 self-canonical 이고 나머지는 그 URL 을 canonical 로 지정한다."
    acceptance: "`pnpm --filter @glowmate/web test:landing-canonical` — 포함관계·근사중복 픽스처 그룹(3개 조합)에서 self-canonical 이 1건이 아니면 실패."
  - id: REQ-6
    statement: "가격 통계(중앙값·최소·최대)는 @glowmate/price-state 판정이 confirmed 인 요금제만으로 계산하고 표본 수 n 을 같은 블록에 출력한다."
    acceptance: "`pnpm --filter @glowmate/web test:landing-stats` — 혼합 픽스처(confirmed 6·estimated 3·conflicted 1)에서 통계값이 confirmed 6건 기준과 불일치하거나 n 미출력이면 실패."
  - id: REQ-7
    statement: "조합 페이지는 revalidate ≤ 3600 으로 재검증되며, 재검증 시 자격(공개 업체 수 ≥ 5)을 재판정해 자격을 상실한 조합은 404 를 반환하고 sitemap 제공자 출력에서 제외된다."
    acceptance: "`pnpm --filter @glowmate/web test:landing-revalidate` — 업체 2건을 hidden_request 로 전이시켜 공개 수를 4로 만든 뒤 W1 무효화 호출 시 60초 내 404 및 제공자 출력 제외를 확인."
  - id: REQ-8
    statement: "D2 enterable 매니페스트와 C7 public_venue 스테이징 스냅샷으로 빌드했을 때 생성 조합 페이지의 커버리지 하한(총 30건 이상, G1에서 제외되지 않은 축마다 1건 이상)을 충족한다."
    acceptance: "`pnpm --filter @glowmate/web smoke:landing --env=staging` — 총 30건 미만이거나 미제외 축 중 0건인 축이 있으면 종료 코드 1(R-7). (FORBID-2·FORBID-6의 짝 — 전 조합을 자격 미달·medical 전량 배제로 처리하면 실패한다)"

# ─── 조건부 금지사항 ────────────────────
forbid:
  - id: FORBID-1
    when:         "조합 키(지역·카테고리·니즈)를 결정하거나 landing-keywords.json 에 행을 추가할 때"
    must_not:     "지역·카테고리·태그 목록의 카테시안 곱을 코드에서 생성하거나, D2 에서 enterable 라벨을 받지 않은 조합을 추가"
    because:      "3구 × 3축 × 8태그만으로 수백 조합이 무검증 생성된다. 검색 수요가 없는 조합 페이지가 대량 생성되면 doorway page 로 판정되어 사이트 전체가 색인에서 제외되고, G2로 확보한 롱테일 자산까지 함께 소멸한다."
    detect:       "단위테스트 — generateStaticParams 반환 키가 매니페스트의 부분집합이고 매니페스트가 D2 enterable 집합의 부분집합인지 검증 + lib/landing 에서 카테시안 조합 패턴 grep 0건."
    on_violation: block_merge
  - id: FORBID-2
    when:         "조합의 visibility='public' 업체 수가 5 미만인 경우(빌드 시점 및 REQ-7 재검증 시점 모두)"
    must_not:     "해당 조합 페이지를 생성·서빙하거나 sitemap 제공자 출력에 등재"
    because:      "업체 1~2곳짜리 페이지는 thin content 로 분류되고, 이런 페이지가 다수를 차지하면 사이트 단위 품질 평가가 하락해 정상 조합 페이지의 순위까지 동반 하락한다. 빌드 시점만 검사하면 C7 재판정·W8 takedown 이후 자격 미달 페이지가 무기한 남는다."
    detect:       "통합테스트 — 경계 픽스처(4건/5건)의 생성·등재 여부 검증 + REQ-7 재검증 e2e(공개 수 감소 후 404)."
    on_violation: block_merge
  - id: FORBID-3
    when:         "tools/ci/check-landing-duplication.mjs 의 유사도 임계 또는 검사 대상 범위를 수정할 때"
    must_not:     "임계를 0.8 초과 값으로 변경하거나, 전수 검사를 표본 검사로 대체하는 옵션(--sample 등)을 도입"
    because:      "표본 선정이 구현자 손에 있으면 '가장 서로 다른 100개'를 고르는 3줄 구현이 계약을 100% 준수하며 CI를 영구 초록으로 만들고, 나머지 수천 페이지가 서로 0.99 유사해도 무방해진다. 임계 완화도 같은 효과를 낸다."
    detect:       "테스트 — 스크립트의 임계 상수가 0.8 인지 assert + CLI 파서에 표본 관련 옵션이 존재하면 실패 + 검사 대상 수가 생성 페이지 수와 일치하는지 assert."
    on_violation: block_merge
  - id: FORBID-4
    when:         "lib/landing 의 빌드 타임 생성 코드가 업체·가격·태그 데이터를 취득할 때"
    must_not:     "packages/db 를 직접 import 하거나 SQL 을 실행하거나 @glowmate/api 의 공개 조회 함수 외 경로로 데이터를 취득"
    because:      "빌드 스크립트는 화면 대상 린트를 우회하기 쉬운 지점이다. 여기서 원본 테이블을 직접 읽으면 C7 공개 판정을 통과하지 못한 업체가 수천 개 정적 페이지에 박제되어 배포되고, 앱 확장 시 동일 로직을 재사용할 수 없다."
    detect:       "F1 boundary job(dependency-cruiser: lib/landing → packages/db 금지) + 통합테스트 — visibility 4종 픽스처에서 public 외 업체가 어떤 생성 페이지 HTML 에도 등장하지 않음을 검증."
    on_violation: block_merge
  - id: FORBID-5
    when:         "조합 페이지의 가격 통계 또는 title·description 의 가격 문구를 생성할 때"
    must_not:     "@glowmate/price-state 판정이 confirmed 가 아닌 요금제를 계산 입력에 포함하거나, 표본 수 n 없이 '평균 OO원'·'최저 OO원' 문구를 출력"
    because:      "'강남 필라테스 가격' 스니펫에 표본 2건짜리 추정 평균이 실리면 사용자는 그것을 시장 가격으로 인식한 뒤 현장에서 배신당한다. 스니펫은 사이트 밖에서 유통되어 회수할 수 없다."
    detect:       "단위테스트 — 통계 함수가 비confirmed 입력을 받으면 throw, 메타 description 생성 함수 출력에 n 표기가 없으면 실패."
    on_violation: block_merge
  - id: FORBID-6
    when:         "매니페스트 행의 카테고리가 medical_wellness 인 경우"
    must_not:     "title·description·본문에 docs/discovery/D4/forbidden_lexicon.yaml 등재 표현을 출력하거나 시술명과 금액을 결합한 문구를 생성"
    because:      "조합 페이지는 검색 노출을 목적으로 만든 문서라 의료광고로 해석될 여지가 가장 크고, 동일 문구가 수백 페이지에 복제되면 단일 신고가 전량 위반으로 확대된다."
    detect:       "렌더 스캔 — medical 조합 픽스처(금칙 표현 포함 케이스 10건 이상 필수)와 실제 생성물 전량의 HTML·메타를 forbidden_lexicon.yaml 로 매칭, 1건이라도 걸리면 실패. 짝 REQ-8이 medical 축 전량 배제를 막는다."
    on_violation: block_merge

# ─── 경계 ───────────────────────────────
out_of_scope:
  - "D2 키워드·enterable 라벨의 추가·수정 (D2 산출물, 읽기 전용)"
  - "@glowmate/price-state 자체 구현 (F6) · 공개 조회 함수 구현 (F5) · 니즈 태그 부여 규칙 (C6·F4)"
  - "리스트·상세 화면 구현 (W2·W3) — 조합 페이지는 두 화면의 컴포넌트를 재사용만 한다"
  - "에디터 리포트 본문·렌더링 (W5). W5 계약의 `blocks:[W4]` 는 허위 엣지이며 본 계약은 W5 를 의존하지 않는다 — W5 측에서 제거 대상"
  - "W1 의 sitemap.ts·canonical 빌더·robots·무효화 엔드포인트 수정 (제공자 파일 추가와 무효화 호출만 허용)"
  - "CI 워크플로 파일 수정 — 검사 스크립트는 apps/web package.json scripts 에 등록해 F1 test job 에서 실행한다"
  - "내부 링크 그래프 재설계 · 페이지네이션 SEO 실험"
rollback:      "PR 1건 revert 후 재빌드. 조합 라우트와 sitemap 제공자가 함께 제거되어 다음 배포에서 sitemap 에서 사라지고 해당 URL 은 W1 규약에 따라 404 를 반환한다. 대량 색인 사고 시에도 robots 차단이 아니라 404 를 유지한다(robots 차단은 색인 제거를 지연시킨다). DB 변경 없음."
done_when:
  - "REQ-1~REQ-8 의 acceptance 명령이 전부 통과하고 apps/web package.json scripts 에 등록됨"
  - "REQ-1 provenance 검사 결과(매니페스트 행 수·rule.lock 해시)가 PR 본문에 기재됨"
  - "생성/제외 리포트 JSON 이 빌드 산출물로 저장되고 제외 사유 코드 표가 문서화됨"
  - "REQ-8 스테이징 스모크 로그(생성 수·축별 분포)가 PR 본문에 첨부됨"
  - "FORBID-1~6 각각의 탐지 수단이 의도적 위반 픽스처에서 실패하는 것을 확인한 기록이 PR 본문에 첨부"
  - "프로덕션 색인 활성화(robots 허용 + sitemap 제출)는 G4 통과 후 별도 배포로 수행하며, 본 PR 머지 시점에는 W1 REQ-5 의 비프로덕션 전체 차단 상태를 유지"
```
