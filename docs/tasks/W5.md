# W5 — 에디터 리포트 렌더링

> 개정 3판 (2차 감사 `docs/audit/round2-webops.md` W5 절 반영)
> 주요 변경: **REQ-8 데드락 해소**(실콘텐츠 5건 하한 → 존재 파일 전건 + 색인 활성화 조건) ·
> REQ-6(c) sitemap 총계를 `/reports/*` 범위로 한정(W1 REQ-8 충돌 제거) · `category_axis` → `venue.category` ·
> D4 인용을 **rule_id 정본**으로 교체 · `visibility` 5값 전수 · 협찬 리포트의 **정상 운영 경로** 신설 ·
> 가격 어휘를 정정 #10(`confirmed`·`conflict`·`low_confidence`·`unavailable`)으로 통일 · gate 를 W1~W4 와 정합화

## 선행 조건 — 상류 계약 개정 (미충족 시 착수 금지)

| # | 필요한 것 | 소유 계약 | 현재 상태 | 확인 방법 |
|---|---|---|---|---|
| 1 | `venue.category` 를 F5 공개 조회 함수 응답에 포함 (medical 판정 입력) | F5-API-LAYER | 미확인 | `getPublicVenueBySlug` 반환 타입에 `category` 존재 |
| 2 | `packages/legal/medical/dist/exported_rules.json` 발행 + `kind=lexicon\|display` rule_id 목록 | D4-MEDICAL-AD-GUARDRAIL | 계약상 존재 | 파일 존재 + `rule_id` 접두 `MED-` 항목 ≥1 |
| 3 | F6 표시 상태 4값(`confirmed`·`conflict`·`low_confidence`·`unavailable`)과 `showNumeric` 규칙 | F6-PRICE-STATE | **어휘 불일치** — F6 본문은 `estimated` 사용, 정본은 정정 #10 | `packages/price-state` export 값 집합 == 정정 #10 4값 |
| 4 | 에디터 리포트 **원고 생산**(방문·촬영·집필) | **소유 태스크 없음 (운영 영역)** | 미해소 | 본 계약은 콘텐츠 0건 상태에서도 판정 가능하도록 REQ-8 을 설계했다 (§REQ-8) |

> 3·4 는 상신 대상이다. 4가 해소되기 전까지 **H6 판정은 불가능하며**, 본 계약의 완료는 "렌더러 완성"이지 "H6 관측 가능"이 아니다.

```yaml
# ─── 식별 ───────────────────────────────
id:            W5-EDITOR-REPORT
dag_id:        W5
title:         에디터 실사 리포트 렌더링 (신뢰 앵커 + SEO 유니크 콘텐츠)
workstream:    web
owner_agent:   dev-web

# ─── 존재 이유 ──────────────────────────
traces_to:     [H6, H3, S3]
why:           "크롤링으로는 만들 수 없는 유일한 자체 콘텐츠 표면이 없으면 H6(리포트 보유 업체의 리드 전환율 우위)을 관측할 대상이 존재하지 않고, SEO 차별화 축도 사라진다."

# ─── DAG ────────────────────────────────
depends_on:    [W1-SEO-FOUNDATION, F5-API-LAYER, F6-PRICE-STATE, D4-MEDICAL-AD-GUARDRAIL]
blocks:        []
parallel_with: [W2-DISCOVERY-LIST, W3-VENUE-DETAIL, W7-CORRECTION-REQUEST, W9-OBSERVABILITY-EVENTS]
gate:          [G1, G2, G3]   # W1~W4 와 동일. Phase 3 는 PRD §4 에 따라 3게이트 통과 후 착수

# ─── 산출물 ─────────────────────────────
deliverable:
  pr_count:    1
  touches:
    - apps/web/src/app/(content)/reports/**
    - apps/web/src/components/report/**
    - apps/web/src/lib/report/**
    - apps/web/src/lib/sitemap-sources/reports.ts   # W1 제공자 규약 (sitemap.ts 수정 금지)
    - content/reports/**
    - apps/web/tests/report/**
    - apps/web/fixtures/report/**
  artifacts:
    - "리포트 목록 /reports · 상세 /reports/[slug] 라우트 (W1 매니페스트 등재)"
    - "프론트매터 스키마 + 빌드타임 검증기 + 제외 디렉터리 규약"
    - "고지 컴포넌트 · 방문 신선도 배지 · 출처 라벨 갤러리"
    - "Article + about(LocalBusiness) JSON-LD (W1 seo 빌더 사용)"
    - "sitemap 제공자 lib/sitemap-sources/reports.ts"
    - "픽스처 7종(정상 / medical / visibility 4종 비공개 / 비-confirmed 가격 / EXIF 불일치 / 원문 복제 / 협찬)"

# ─── 요구사항 ───────────────────────────
requirements:
  - id: REQ-1
    statement: >
      content/reports/*.mdx 중 프론트매터 검증을 통과한 파일 전부가 /reports/[slug] 정적 경로로
      생성되고 생성 경로 수와 통과 파일 수의 차이가 0건이며, 리포트 라우트 세그먼트의
      `export const revalidate` 값이 3600 이하로 선언된다.
    acceptance: >
      `pnpm test:report-routes` — route manifest 의 /reports/* 경로 수 == 통과 파일 수 assert,
      `node tools/ci/check-render-strategy.mjs` 가 revalidate > 3600 선언을 exit 1 로 거부.

  - id: REQ-2
    statement: >
      모든 리포트 프론트매터는 필수 8필드(slug, venue_id, editor_id, visited_at(YYYY-MM-DD),
      funding, receipt_ref, images[].source, images[].captured_at)를 갖고, 1개라도 누락되면
      해당 파일이 빌드 대상에서 제외되며 검증기가 누락 필드명을 출력한다.
    acceptance: >
      `pnpm test:report-schema` — 필드별 결손 픽스처 8건 각각에서 해당 slug 의 라우트가 생성되지 않고
      누락 필드명이 출력됨을 assert.

  - id: REQ-3
    statement: >
      리포트 상세에서 고지 블록(자비 방문 · 대가 미수령 · 업체 미접촉)이 본문 첫 문단보다 DOM 상 앞에
      렌더되고 `data-disclosure` 속성을 갖는다.
    acceptance: >
      컴포넌트 테스트 — [data-disclosure] 노드의 DOM 순서 인덱스 < [data-report-body] 첫 자식 assert.

  - id: REQ-4
    statement: >
      visited_at 이 렌더 기준일로부터 365일을 초과하면 "N개월 전 방문" 신선도 배지를, 365일 이하이면
      방문 연월을 렌더한다.
    acceptance: >
      단위테스트 — 경계 픽스처 3건(364 / 365 / 366일)의 렌더 문자열 스냅샷 일치.

  - id: REQ-5
    statement: >
      리포트 상세는 W1 seo 빌더를 통해 JSON-LD(@type=Article, about=@type=LocalBusiness,
      datePublished, author) 1개를 출력하고 ajv 스키마 검증 에러가 0건이다.
    acceptance: >
      `pnpm test:jsonld -- reports` — 스키마 검증 에러 카운트 0 assert.

  - id: REQ-6
    statement: >
      sitemap 의 `/reports/*` 항목 수가 공개 대상 리포트 수(대상 업체 `visibility='public'` 이고
      프론트매터 검증 통과)와 정확히 일치한다. 리포트 URL 과 업체 상세 링크는 lib/routes 의
      buildPath() 산출값으로만 생성된다.
    acceptance: >
      `pnpm test:sitemap -- reports` — sitemap 출력에서 `/reports/` 접두 항목 수 == 공개 리포트 수
      (다른 제공자 항목은 계산에서 제외), 그리고 `pnpm ci:check-url-source` 검출 0건.

  - id: REQ-7
    statement: >
      대상 업체가 `visibility='public'` 인 공개 픽스처 10건에서 (a) 업체 상세 링크 렌더율 100%,
      (b) F6 판정이 `confirmed` 인 가격의 숫자 렌더율 100% 이며, 모든 가격 노드가 F6 판정값을
      `data-price-state` 속성으로 동반한다.
    acceptance: >
      `pnpm test:report-render` — 10건 전부에서 상세 href ≥1개, confirmed 가격 숫자 노드 ≥1개,
      data-price-state 결손 0건 (전량 미표기 구현 시 실패).

  - id: REQ-8
    statement: >
      스테이징 배포에서 content/reports 의 검증 통과 파일 **전건**이 200 을 반환하고 각 응답 HTML 에
      고지 블록·방문일·JSON-LD 3요소를 포함하며, 통과 파일이 0건인 경우 /reports 목록 라우트가
      200 과 함께 sitemap 제공자 출력 0건을 반환한다.
    acceptance: >
      `pnpm test:e2e:reports-staging` — (a) 통과 파일 목록을 빌드 산출물에서 읽어 전건 fetch,
      status 200 및 3요소 존재 assert, (b) 0건 케이스에서 목록 200 + sitemap `/reports/` 항목 0건 assert.

# ─── 조건부 금지사항 ────────────────────
forbid:
  - id: FORBID-1
    when:         리포트 프론트매터의 funding 이 "self" 가 아니거나, funding="self" 인데 receipt_ref 가 가리키는 결제 증빙 파일이 저장소에 없는 경우
    must_not:     해당 리포트를 /reports/* 라우트로 생성하거나 sitemap 제공자 출력에 포함
                  (해당 파일을 content/reports/_excluded/ 로 이동해 두는 것은 규칙 안의 정상 경로이며 빌드는 성공한다)
    because:      대가를 받은 콘텐츠가 "자비 방문 실사" 고지와 같은 템플릿으로 노출되면 표시광고법상 기만적 표시이고, funding 이 자기신고로만 남으면 값을 적는 것만으로 규칙이 무력화된다. 반대로 빌드 전체를 실패시키면 협찬 제안을 받은 첫 사례에서 규칙이 우회되므로, 제외 경로를 규칙 안에 둔다
    detect:       빌드타임 검증 CI — (a) funding!="self"·receipt_ref 결손 픽스처가 content/reports 루트에 있으면 exit 1, (b) 동일 파일이 _excluded/ 에 있으면 빌드 성공 + 라우트 0건 + sitemap 0건 assert
    on_violation: block_merge

  - id: FORBID-2
    when:         리포트 대상 업체의 `venue.category` 가 D4 `scope.yaml` 의 medical_wellness 업종 enum 에 속하는 경우
    must_not:     `exported_rules.json` 의 `kind=lexicon` rule_id(MED-L-*) 패턴에 매치되는 표현 또는 `kind=display` 허용 포맷을 벗어난 가격 표기를 본문·title·description·JSON-LD 에 렌더
    because:      의료광고법상 치료효과 보장·암시 표현과 비급여 진료비의 유인성 표시는 규제 대상이며, 적발 시 해당 축을 넘어 서비스 전체가 중단될 수 있다
    detect:       CI 콘텐츠 린트 — `packages/legal/medical/dist/exported_rules.json`(package.json semver 핀)을 로드해 medical 리포트의 본문·메타·JSON-LD 문자열을 대조, 히트 1건 이상이면 exit 1 (금칙 케이스 픽스처 5건 필수). 축 판정 불가 시 기본 동작은 `hide`
    on_violation: block_merge

  - id: FORBID-3
    when:         본문에 표기하려는 가격의 F6 판정이 `confirmed` 가 아닌 경우(`conflict` · `low_confidence` · `unavailable`)
    must_not:     F6 의 showNumeric=false 를 무시하고 숫자를 서술문에 포함하거나, `conflict` 상태에서 한쪽 값만 단독 표기, 또는 리포트 안에서 가격 상태를 자체 판정
    because:      W2·W3·W4 가 3중으로 막은 "추정치의 확정 표기"가 가장 신뢰받는 콘텐츠 표면에서만 뚫리면, 사용자는 리포트에서 읽은 틀린 가격으로 업체를 고르고 "가격이 틀린 사이트" 평판은 데이터를 고쳐도 회복되지 않는다
    detect:       `pnpm test:report-render` — 상태 4종 픽스처에서 (a) non-confirmed 가격 노드에 배지 노드 부재 시 실패, (b) conflict 픽스처에서 값 노드 2개 미만이면 실패, (c) `check-price-state-single-source.mjs`(F6 REQ-2) 스캔에 apps/web/src/lib/report/** 포함
    on_violation: block_merge

  - id: FORBID-4
    when:         리포트 대상 업체의 `visibility` 가 'public' 이 아닌 4값(`needs_review`·`hidden_quality`·`hidden_request`·`hidden_legal`) 중 하나인 경우
    must_not:     그 업체의 가격 수치·상세 링크를 리포트 페이지에 렌더하거나 리포트 URL 을 sitemap 에 출력
    because:      G4 가 비공개로 묶은 저신뢰 데이터와 W8 이 삭제 요청으로 내린 업체가 리포트 경로로 우회 색인되면 게이트와 삭제 요청이 동시에 무력화되고, 색인 캐시에는 수 주간 잔존해 정정해도 즉시 사라지지 않는다
    detect:       통합테스트 — 비공개 4값 픽스처 각각에서 (a) 가격 노드 0개(숫자 정규식 0건), (b) 상세 href 0개, (c) sitemap `/reports/` 출력 0건 assert
    on_violation: block_merge

  - id: FORBID-5
    when:         `source="editor"` 이미지의 EXIF 촬영일이 visited_at ±7일 범위에 있는 항목이 0장이거나, 본문과 대상 업체 source_record 원문의 5-gram Jaccard 유사도가 0.30 을 초과하는 경우
    must_not:     해당 파일을 editor_report 타입 콘텐츠로 빌드에 포함
    because:      방문하지 않은 곳을 방문했다고 쓰는 것이 목표 건수를 채우는 가장 쉬운 경로인데, 등록 editor_id 와 사진 1장만 요구하면 생성문이 100% 통과한다. 허구 리포트는 H6 코호트를 오염시키고 발각 시 서비스 신뢰가 회복 불가능하게 손상된다
    detect:       빌드타임 검증 CI — EXIF DateTimeOriginal 파싱 + 원문 유사도 계산. 위반 픽스처 2종(EXIF 불일치·원문 복제)에서 각각 exit 1 확인
    on_violation: block_merge

  - id: FORBID-6
    when:         리포트 이미지 항목의 source 값이 "editor" 가 아닌 경우(업체 제공·소스 인용)
    must_not:     에디터 촬영물과 동일한 갤러리 컨테이너에 출처 라벨 없이 병렬 배치
    because:      실사 사진과 업체 홍보 이미지가 구분 없이 섞이면 "직접 방문했다"는 주장의 검증 가능성이 사라지고, 한 장이라도 적발되면 리포트 전체의 진위가 의심받는다
    detect:       컴포넌트 테스트 — source!="editor" 픽스처에서 [data-image-source] 캡션 노드 존재 및 editor 이미지와 다른 컨테이너 소속 assert
    on_violation: block_merge

# ─── 경계 ───────────────────────────────
out_of_scope:
  - 리포트 원고 작성·촬영 등 콘텐츠 생산 (운영 영역, 소유 태스크 부재 — 선행 조건 표 #4)
  - 리포트 작성용 CMS·어드민 UI
  - W3 업체 상세의 "에디터 리포트 있음" 배지 — W3 소관
  - 리포트 열람 이벤트·코호트 속성 산출 — W9 소관
  - W1 의 sitemap.ts · routes · seo 빌더 수정 (제공자 파일 추가만)
  - F6 판정 규칙 정의·수정 (소비만. 자체 판정 구현 금지 — 정정 #10)
  - D4 규칙 내용 변경 (rule_id 인용만)
  - DB 스키마 변경 — F2a/F2b 소관
  - packages/api 의 조회 함수 구현·변경 — F5 소관 (호출만)

rollback: >
  PR revert 후 재빌드. /reports/* 라우트·content/reports·sitemap 제공자가 함께 제거되며 W1 sitemap.ts 는
  무수정이므로 부분 롤백이 안전하다. DB 변경이 없어 마이그레이션 되돌림은 불필요하고, 이미 색인된
  리포트 URL 은 revert 배포 후 Search Console 제거 요청으로 처리한다.

done_when:
  - REQ-1~REQ-8 acceptance 가 CI 에서 전부 통과
  - FORBID-1~6 각각의 위반 픽스처가 커밋되고 detect 가 차단함을 PR 본문에 기록
  - 픽스처 7종이 저장소에 존재 (visibility 4값 전수 포함)
  - 검증 통과 리포트가 0건인 동안에는 sitemap `/reports/` 항목이 0건이며, 프로덕션 색인 활성화는 ⛔G4 통과 후 별도 배포로 수행
  - 선행 조건 표 1~3 이 충족되었음을 확인 (미충족 시 착수 금지)
  - touches 밖 파일 변경 0건 (CI path guard 통과)
```
