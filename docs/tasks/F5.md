# F5 — 공용 API 레이어 패키지 (웹/앱 공용 · DB 접근 단일 경계)

> 계약 규격: [02-task-contract-spec.md](../02-task-contract-spec.md) §1
> 신설: 2026-08-03 (`docs/audit/W1-O3-audit.md` — PRD §2가 요구한 공용 API 레이어의 소유 태스크 부재)

```yaml
# ─── 식별 ───────────────────────────────
id:            F5-API-LAYER
dag_id:        F5
title:         공용 API 레이어 패키지 (packages/api — 공개면 조회 단일 경로)
workstream:    foundation
owner_agent:   dev-web

# ─── 존재 이유 ──────────────────────────
traces_to:     [H4, G4, S4]
why:           "웹 라우트가 각자 DB를 조회하면 공개 판정(public_venue) 우회 경로가 표면 수만큼 생기고,
                Expo 앱 확장 시 동일 쿼리를 재구현해야 한다."

# ─── DAG ────────────────────────────────
depends_on:    [F1-REPO-SCAFFOLD, F2a-CORE-SCHEMA, F2b-QUALITY-SCHEMA]
               # F2b 추가 사유: 본 계약의 핵심 보증(공개면 조회는 public_venue 경유)은
               # F2b 가 뷰와 롤을 만들기 전에는 검증 대상이 존재하지 않는다.
blocks:        [W1-SEO-FOUNDATION, W2-DISCOVERY-LIST, W3-VENUE-DETAIL, W4-COMBO-LANDING, W5-EDITOR-REPORT, W7-CORRECTION-REQUEST, W9-OBSERVABILITY-EVENTS]
parallel_with: [F4-NEED-TAG-ONTOLOGY, F6-PRICE-STATE, DS3-BASE-WIRING]
gate:          null                  # Phase 1은 게이트와 무관하게 선행 가능

# ─── 산출물 ─────────────────────────────
deliverable:
  pr_count:    1
  touches:
    - packages/api/src/**
    - packages/api/test/**
    - packages/api/package.json
    - packages/db/src/pool.ts                # 커넥션 풀 싱글턴 (F1 스캐폴드 확장)
    - .dependency-cruiser.cjs                # no-db-outside-api · no-framework-in-api 규칙 추가만
    - .github/workflows/ci.yml               # api-integration job 추가만
    - .github/ci-budget.json                 # api-integration 예산 항목 추가만
  artifacts:
    - "packages/api — 프레임워크 비의존 공개 조회 함수 (listPublicVenues · getPublicVenueBySlug)"
    - "zod 응답 계약 스키마 + 컬럼 화이트리스트 직렬화"
    - "폐쇄 에러 타입 enum (NotFound · Unavailable · ContractViolation)"
    - "커넥션 풀 싱글턴"
    - "dependency-cruiser 경계 규칙 2종 + 위반 픽스처"

# ─── 요구사항 ───────────────────────────
requirements:
  - id: REQ-1
    statement: >
      packages/db 및 postgres 드라이버(pg · postgres · drizzle-orm · @prisma/client)를 import 하는 패키지가
      packages/api 하나뿐이며, 리포지토리 전체(apps/web/src/app/api/** 포함)의 위반 건수가 0 이다.
    acceptance: "CI job `boundary` — depcruise 규칙 `no-db-outside-api` 위반 0건 + 픽스처 `packages/api/test/fixtures/violation-route-imports-db.ts` 포함 시 exit ≠ 0."

  - id: REQ-2
    statement: "packages/api/src 전체에서 next/* · react · react-dom import 건수가 0 이다 (Expo 재사용 전제)."
    acceptance: "CI job `boundary` — depcruise 규칙 `no-framework-in-api` 위반 0건 + 위반 픽스처 exit ≠ 0."

  - id: REQ-3
    statement: >
      공개 조회 함수(packages/api/src/public/**)의 SQL FROM 절이 public_venue 뷰로만 구성되고,
      base 테이블(venue · price_plan · venue_quality_judgement)을 FROM 에 사용하는 공개 함수가 0건이다.
    acceptance: >
      `pnpm test:api-public` — (a) 정적 검사: public/** 의 SQL 문자열에 `FROM venue`·`FROM price_plan`·
      `visibility` 리터럴이 검출되면 exit 1, (b) 통합테스트: hidden 4종 픽스처 각 1건에 대해
      listPublicVenues·getPublicVenueBySlug 결과가 0건/null.

  - id: REQ-4
    statement: >
      모든 공개 함수 반환값이 zod 스키마 파싱을 통과한 객체이며, 스키마에 선언되지 않은 뷰 컬럼은
      응답에서 제거된다(strip). 필수 키는 optional 로 선언되지 않는다.
    acceptance: >
      `pnpm test:api-contract` — (a) 뷰에 임시 컬럼을 추가한 픽스처에서 응답 키 집합이 불변,
      (b) 스키마 메타테스트: 필수 키 목록 전건에 대해 `isOptional() === false`,
      (c) 소스에 `.passthrough(`·`.optional()` 이 필수 키에 적용되면 exit 1.

  - id: REQ-5
    statement: >
      visibility='public' 픽스처 20건에 대해 listPublicVenues 가 20건 전건을 반환하고,
      getPublicVenueBySlug 가 20건 전건에 대해 non-null 을 반환한다 (정상 조회 하한).
    acceptance: "`pnpm test:api-public` — 반환 행수가 정확히 20 이 아니거나 null 이 1건이라도 있으면 exit 1."

  - id: REQ-6
    statement: >
      모든 조회 테스트가 DB mock·인메모리 스텁 없이 실제 PostgreSQL 16 + PostGIS 컨테이너에
      F2a·F2b 마이그레이션을 적용한 상태에서 실행된다.
    acceptance: "CI job `api-integration` — 컨테이너 기동 후 마이그레이션 적용, packages/api/test 에서 `jest.mock('pg'` · `vi.mock('pg'` 류 패턴 검출 시 exit 1."

  - id: REQ-7
    statement: "프로세스당 커넥션 풀 인스턴스가 정확히 1개이며, 공개 함수 50회 동시 호출 시 생성된 풀 수가 1이다."
    acceptance: "`pnpm test:api-pool` — 풀 팩토리 호출 카운터가 50회 동시 호출 후에도 1, max 커넥션 초과 에러 0건."

# ─── 조건부 금지사항 ────────────────────
forbid:
  - id: FORBID-1
    when: "packages/api 밖의 파일(apps/web/** · apps/web/src/app/api/** · packages/ui/**)이 packages/db 또는 postgres 드라이버를 import 하는 경우"
    must_not: "API 레이어를 우회한 직접 DB 접근"
    because: >
      라우트 핸들러가 직접 쿼리를 쓰면 public_venue 강제가 표면마다 다시 협상된다. W3 상세 API 하나만
      뚫려도 C7 이 비공개로 돌린 업체의 가격이 URL 하나로 계속 노출되고, takedown 이행이 실패한다.
    detect: "CI job `boundary` — 규칙 `no-db-outside-api` + 위반 픽스처가 반드시 실패하는지 확인하는 메타테스트"
    on_violation: block_merge

  - id: FORBID-2
    when: "packages/api/src/public/** 의 쿼리가 public_venue 뷰 대신 base 테이블을 조회하거나 visibility 조건을 자체 WHERE 절로 기술하는 경우"
    must_not: "공개 판정 조건을 API 레이어에서 복제"
    because: >
      공개 판정이 2곳이 되면 F2b 뷰가 바뀌어도 API 는 옛 조건으로 계속 응답한다. C7 재판정·O2 오버라이드·
      W7 takedown 중 어느 것도 반영되지 않은 데이터가 서빙되는데, 어느 계약도 위반되지 않는다.
    detect: "`pnpm test:api-public` — 정적 SQL 검사 + hidden 4종 픽스처 통합테스트(REQ-3)"
    on_violation: block_merge

  - id: FORBID-3
    when: "공개 조회 함수 시그니처 또는 환경변수 분기에 includeHidden · preview · debug · bypassGate · allVisibility 류 옵션을 추가하는 경우"
    must_not: "비공개 레코드를 반환할 수 있는 파라미터·환경 분기 도입"
    because: >
      프리뷰 옵션은 반드시 프로덕션 코드 경로에 남고, 파라미터 하나로 비공개 업체 상세가 노출된다.
      삭제 요청으로 내린 업체가 그 경로로 계속 조회되면 이행 실패가 되어 법적 리스크로 직결된다.
    detect: "`pnpm test:api-public` — 파라미터 fuzz 20종(위 5개 키 × 대소문자·별칭)에 대해 응답 행수 불변 assert + 소스 grep 사전"
    on_violation: block_merge

  - id: FORBID-4
    when: "REQ-4 의 zod 계약 테스트가 실패한 상태에서 스키마를 수정하는 경우"
    must_not: "필수 키를 optional 로 바꾸거나 `.passthrough()`·`.catchall()` 을 추가해 검증을 통과시키는 것"
    because: >
      느슨한 스키마는 F2b 가 만든 confidence·visibility 누락을 조용히 통과시킨다. 그러면 W3 가 정의한
      "검증 실패 시 렌더 0건"이 영원히 발동하지 않고, 결손 데이터가 확정 가격 화면으로 렌더된다.
    detect: "`pnpm test:api-contract` — 필수 키 목록(테스트 소스 하드코딩)에 대해 isOptional()===false assert + passthrough/catchall grep. 필수 키 목록 변경은 packages/api CODEOWNERS 승인 리뷰(승인자 ≠ 작성자) 필요."
    on_violation: block_merge

  - id: FORBID-5
    when: "DB 커넥션 실패 또는 쿼리 예외가 발생한 경우"
    must_not: "빈 배열·기본 객체를 정상 응답으로 반환하거나 예외를 삼키는 것 (폐쇄 에러 타입 Unavailable 로 전파할 것)"
    because: >
      DB 장애 시 전 업체가 사라진 사이트가 200 으로 서빙되면 크롤러가 대량 404/빈 페이지를 색인 제거하고,
      5xx 가 없으니 O3 알림도 발화하지 않는다. 장애가 조용히 SEO 자산을 삭제한다.
    detect: "`pnpm test:api-errors` — 커넥션 강제 종료 후 호출 시 Unavailable 예외 throw, 빈 배열 반환 0건 assert"
    on_violation: block_merge

# ─── 경계 ───────────────────────────────
out_of_scope:
  - "Next.js 라우트 핸들러 작성 (W1·W2·W3 소관) — 본 태스크는 핸들러가 호출할 함수만 제공"
  - "필터·정렬·페이지네이션 파라미터 설계 (W2)"
  - "가격 상태 판정 규칙 (F6) — API 는 원시 필드만 반환하고 판정하지 않는다"
  - "public_venue 뷰 정의 변경 (F2b 소관. 컬럼이 부족하면 작업을 중단하고 F2b 변경을 요청한다)"
  - "인증·세션·레이트리밋 (W6·W9 이벤트 엔드포인트 소관)"
  - "캐싱·ISR 전략 (W1)"
  - "이벤트 쓰기 경로 (W6/W9)"

rollback: >
  `git revert <merge-sha>`. F5 시점에 packages/api 를 소비하는 라우트가 없으므로(W1 미착수) 하위 영향이 없다.
  DB 스키마 변경 없음. depcruise 규칙 2종과 api-integration job 은 동일 revert 에 포함되며,
  브랜치 보호 필수 체크에서 api-integration 을 함께 해제한다.

done_when:
  - "`pnpm test:api-public && pnpm test:api-contract && pnpm test:api-errors && pnpm test:api-pool` 전부 exit 0"
  - "위반 픽스처 5종(라우트에서 db import / api 에서 next import / base 테이블 FROM / preview 파라미터 / 필수 키 optional)이 각각 대응 검사를 실패시킴을 확인"
  - "REQ-5 의 공개 픽스처 20건이 전건 반환됨(전량 차단 구현 방지 확인)"
  - "api-integration job 이 실제 PostGIS 컨테이너에서 실행되고 mock 검출 0건"
  - "F1 의 8개 job 이 여전히 green 이고 ci-budget 합계 상한 위반 없음"
  - "packages/api/README 에 공개 함수 목록 · 반환 스키마 · 에러 타입 3종 표 포함"
```
