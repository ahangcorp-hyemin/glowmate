# W3 — 업체 상세(가격 비교 블록)

> 개정 2026-08-03 — `docs/audit/W1-O3-audit.md` W3 항목 및 `03-task-dag.md` 정정 #1·#2·#3·#5·#9 반영.
> 주요 변경: **공개 판정 우회 금지 FORBID 신설(감사 최우선 지적)** · 요금제 8행 절단 폐지(전량 렌더) · `lib/price-state.ts` 자체 생성 폐기 → F6 의존 · LocalBusiness JSON-LD 소유권 인수 · 고신뢰/medical 렌더 하한 REQ 신설(원칙 2.5) · 실환경 하한 REQ 신설(R-7).

```yaml
# ─── 식별 ───────────────────────────────
id:            W3-VENUE-DETAIL
title:         "업체 상세 — 회당 단가 비교 블록 · 신뢰도/충돌 표기 · LocalBusiness JSON-LD"
workstream:    web
owner_agent:   dev-web

# ─── 존재 이유 ──────────────────────────
traces_to:     [H4, S1]
why:           "C4·F6가 산출한 회당 단가와 가격 상태를 사용자가 판단 가능한 형태로 보여주는 유일한 화면이며, 이것이 없으면 North Star(상세→리드 액션)와 H4를 관측할 대상이 존재하지 않는다."

# ─── DAG ────────────────────────────────
depends_on:    [W1-SEO-FOUNDATION, F2a-CORE-SCHEMA, F2b-QUALITY-SCHEMA, F5-API-LAYER, F6-PRICE-STATE, C4-PRICE-NORMALIZER, C7-QUALITY-GATE, D4-MEDICAL-AD-GUARDRAIL, DS4-PRICE-COMPARE-CARD, DS6-A11Y-GATE]
blocks:        [W6-LEAD-TRACKING]
parallel_with: [W2-DISCOVERY-LIST, W7-CORRECTION-REQUEST]
gate:          [G1, G2, G3]

# ─── 산출물 ─────────────────────────────
deliverable:
  pr_count:    1
  touches:
    - apps/web/src/app/(venue)/**                  # W1 매니페스트가 도출하는 상세 라우트 세그먼트
    - apps/web/src/app/api/venues/[slug]/route.ts  # @glowmate/api 위임 전용 얇은 핸들러
    - apps/web/src/components/venue/**
    - apps/web/src/lib/venue-jsonld.ts             # LocalBusiness JSON-LD 빌더 (W1에서 이관)
    - apps/web/src/lib/sitemap-sources/venues.ts   # W1 규약에 따른 제공자 추가 (sitemap.ts 수정 금지)
    - apps/web/tests/venue/**
    - apps/web/fixtures/venue/**
    - apps/web/package.json                        # scripts 항목 추가만
  artifacts:
    - "상세 라우트(SSG/ISR, revalidate ≤ 3600)"
    - "가격 비교 블록(DS4 PriceCompareCard 사용) — 상태 4종 시각 규격"
    - "가격 충돌 병기 뷰"
    - "GET /api/venues/{slug} 얇은 핸들러 + zod 응답 스키마(@glowmate/api 타입 파생)"
    - "LocalBusiness JSON-LD 빌더 + priceRange 조건 테스트"
    - "고신뢰·medical 렌더 커버리지 골든셋 및 스테이징 스모크"

# ─── 요구사항 ───────────────────────────
requirements:
  - id: REQ-1
    statement: "가격 비교 블록은 해당 업체의 요금제를 절단 없이 전량, 회당 단가 오름차순으로 렌더하며 각 행에 원문 문구·총액·횟수·회당 단가·수집일 5개 필드를 출력한다."
    acceptance: "`pnpm --filter @glowmate/web test:price-block` — 요금제 20건 픽스처에서 렌더 행 수 20, 각 행 5개 data-field 존재, 정렬 순서 스냅샷 일치."
  - id: REQ-2
    statement: "각 요금제 행의 data-price-state 값은 @glowmate/price-state 판정 결과(confirmed·estimated·conflicted·unknown)와 일치하며, confirmed 가 아닌 행은 배지와 근거 문구를 함께 출력한다."
    acceptance: "`pnpm --filter @glowmate/web test:price-state` — 상태 4종 픽스처에서 속성값 불일치 또는 배지 누락이 1건이라도 있으면 실패."
  - id: REQ-3
    statement: "price_conflict 테이블에 참조된 요금제는 양쪽 price_plan 의 값과 소스명을 동시에 출력하고, 특정 값에만 강조 스타일(data-emphasis=\"primary\")을 부여하지 않는다."
    acceptance: "`pnpm --filter @glowmate/web test:price-conflict` — 충돌 픽스처에서 값 노드 2개 미만이거나 소스명 2개 미만이거나 data-emphasis=\"primary\" 노드가 1개 이상이면 실패."
  - id: REQ-4
    statement: "요금제가 0건인 공개 업체도 상세 페이지가 200 으로 렌더되며, 가격 영역 텍스트의 숫자 매칭이 0건이다."
    acceptance: "`pnpm --filter @glowmate/web test:price-empty` — 상태 코드 200 이 아니거나 가격 영역에 숫자가 1건이라도 있으면 실패."
  - id: REQ-5
    statement: "LocalBusiness JSON-LD 는 name·address·geo·url 4개 필수 필드를 항상 포함하고, priceRange 필드는 confirmed 요금제가 2건 이상일 때만 존재한다."
    acceptance: "`pnpm --filter @glowmate/web test:venue-jsonld` — ajv 스키마로 필수 4필드 검증 + confirmed 0/1/3건 3케이스의 priceRange 키 유무가 기대값과 일치."
  - id: REQ-6
    statement: "상세 응답 zod 스키마는 @glowmate/api 가 export 하는 타입과 1:1 대응하며, 필수 필드가 optional 로 선언된 항목 수가 0이다."
    acceptance: "`pnpm --filter @glowmate/web test:venue-contract` — 타입 대조 테스트에서 optional 로 완화된 필수 필드가 1건이라도 있으면 실패, 스키마 위반 픽스처 4종은 404 또는 에러 바운더리로 귀결."
  - id: REQ-7
    statement: "골든셋 40건(고신뢰 confirmed 업체 20건 + D4 허용 포맷 medical 업체 20건)에서 숫자 가격이 실제로 렌더되는 비율이 100% 이다."
    acceptance: "`pnpm --filter @glowmate/web test:price-render-coverage` — 1건이라도 미렌더면 실패. 골든셋 표집: C7 스냅샷에서 조건을 만족하는 모집단을 venue_id 해시 오름차순으로 정렬해 상위 40건을 선정하고, 동일 절차로 뽑은 별도 홀드아웃 20건은 저장소에 커밋하지 않고 스테이징 스모크에서만 사용한다(R-6). (FORBID-1·FORBID-3의 짝)"
  - id: REQ-8
    statement: "C7 public_venue 스테이징 스냅샷 상위 50개 업체 상세에서, confirmed 요금제를 보유한 업체의 숫자 가격 렌더 비율이 100% 이고 렌더 실패(5xx) 건수가 0이다."
    acceptance: "`pnpm --filter @glowmate/web smoke:venue --env=staging` — 미렌더 또는 5xx 가 1건이라도 있으면 종료 코드 1. 홀드아웃 20건을 포함해 실행한다(R-7)."

# ─── 조건부 금지사항 ────────────────────
forbid:
  - id: FORBID-1
    when:         "apps/web/src/app/api/venues/[slug]/route.ts 또는 (venue) 세그먼트가 업체 데이터를 취득할 때"
    must_not:     "@glowmate/api 의 공개 조회 함수 외의 경로(packages/db 직접 import · SQL 문자열 · visibility 조건 자체 구현 · preview/debug/includeHidden 파라미터로 비공개 레코드 반환)를 사용"
    because:      "상세는 가격이 실제로 노출되는 화면이라 게이트 우회의 1순위 표면이다. hidden_legal·hidden_request 로 내린 업체가 직링크로 200을 반환하면 W8의 takedown 이행이 무효가 되고 법적 리스크가 그대로 실현된다."
    detect:       "F1 boundary job(dependency-cruiser) + 통합테스트 — visibility 4종 픽스처와 파라미터 fuzz 20종에서 public 외 레코드가 응답·렌더에 등장하면 실패."
    on_violation: block_merge
  - id: FORBID-2
    when:         "요금제의 @glowmate/price-state 판정이 confirmed 가 아닌 경우"
    must_not:     "confirmed 행과 동일한 시각 표현으로 렌더하거나, 그 값을 페이지 상단 대표 가격·제목·메타 description·openGraph 문구 등 요금제 행 밖 표면에 숫자로 노출"
    because:      "추정치가 확정가처럼 보이면 사용자는 그 숫자를 믿고 방문했다가 다른 금액을 듣는다. 특히 메타·OG 문구는 검색 스니펫으로 사이트 밖에 유통되어 배지·설명 맥락이 사라진 채 확정 가격으로 소비되고 회수할 수 없다."
    detect:       "단위테스트 — 비confirmed 픽스처의 상태 속성·배지 검증 + 렌더된 title·description·og:*·헤더 영역 문자열에서 비confirmed 요금제 금액과 일치하는 숫자 검출 시 실패."
    on_violation: block_merge
  - id: FORBID-3
    when:         "요금제가 price_conflict 테이블 행에 참조된 경우(C7 reason_codes 에 PRICE_CONFLICT 포함)"
    must_not:     "한쪽 값을 대표값으로 선택해 단독 노출하거나 두 값의 평균·중앙값을 계산해 하나의 숫자로 표기"
    because:      "근거 없는 선택이나 평균은 어느 소스에도 없는 새 숫자를 만든다. 사용자가 업체에 확인했을 때 어떤 값도 맞지 않는 상태가 되어 오정보 신고(S5)로 직결된다."
    detect:       "통합테스트 — 충돌 픽스처에서 (a) 렌더된 숫자 집합이 입력 소스 값들의 부분집합인지, (b) 해당 요금제의 렌더 값 노드가 2개 이상인지 둘 다 검증(하나라도 실패 시 block)."
    on_violation: block_merge
  - id: FORBID-4
    when:         "category 가 medical_wellness 인 업체 상세를 렌더할 때"
    must_not:     "시술·진료 항목명과 금액을 하나의 문구로 결합하거나 docs/discovery/D4/forbidden_lexicon.yaml 등재 표현(치료 효과·최상급·유인)을 페이지·메타에 출력"
    because:      "비급여 진료비의 비교·유인 표시와 치료 효과 표방은 의료광고법 규제 대상이며, 위반 시 해당 축이 아니라 서비스 전체가 중단될 수 있다."
    detect:       "렌더 스캔 — medical 픽스처 20건(금칙 표현 포함 10건)의 HTML 전문과 메타 태그를 forbidden_lexicon.yaml 정규식으로 매칭, 1건이라도 걸리면 실패. 짝 REQ-7이 medical 전량 숨김을 막는다."
    on_violation: block_merge
  - id: FORBID-5
    when:         "@glowmate/api 응답이 zod 검증에 실패했거나 total_amount·session_count·price_per_session 중 하나 이상이 결손인 경우"
    must_not:     "0·'-'·이전 빌드 캐시값 등 기본값으로 결손 필드를 채워 렌더하거나, 검증을 통과시키기 위해 필수 필드를 optional 로 완화"
    because:      "C4 파서 회귀가 화면에서 정상처럼 보이면 아무도 알아채지 못한 채 틀린 가격이 며칠간 색인·노출된다. 스키마 완화는 그 은폐를 영구화한다."
    detect:       "계약 테스트 — 결손 픽스처 4종이 404/에러 바운더리로 귀결되고 숫자 출력 0건 + REQ-6의 optional 완화 검사(필수 필드 optional 0건)."
    on_violation: block_merge
  - id: FORBID-6
    when:         "가격 비교 블록의 요금제 행 데이터 경로를 구현할 때"
    must_not:     "useEffect·클라이언트 fetch·동적 import 로만 요금제 행을 채워 첫 HTML 에서 비어 있게 구현"
    because:      "가격이 첫 HTML 에 없으면 크롤러가 수집하지 못해 '강남 필라테스 가격' 류 키워드(H3)에서 밀리고, W9가 정의할 가격 블록 노출 코호트가 실제 노출과 어긋나 H4 판정이 불가능해진다."
    detect:       "CI — JS 비활성 fetch 로 얻은 상세 HTML 의 요금제 행 수와 회당 단가 문자열이 픽스처와 일치하는지 검증."
    on_violation: block_merge

# ─── 경계 ───────────────────────────────
out_of_scope:
  - "@glowmate/price-state 모듈 자체 생성·수정 — F6-PRICE-STATE 소유. 없으면 착수 대기(자체 판정 로직 작성 금지)"
  - "공개 조회 함수·visibility 필터 구현 — F5-API-LAYER 소유"
  - "3050 접근성 임계 검사 — DS6-A11Y-GATE 게이트에 위임. W3는 DS4 컴포넌트를 사용만 한다"
  - "리드 액션 이벤트·상세 조회 이벤트 — W6·W9 소유. W3는 CTA 의 data 속성만 제공"
  - "가격 정규화·신뢰도 산출 규칙 (C4) — 화면은 산출값을 표시만 한다"
  - "에디터 리포트 렌더링(W5) 및 상세의 리포트 배지"
  - "W1 의 canonical·sitemap.ts·robots·라우트 빌더 수정 (제공자 파일 추가만 허용)"
rollback:      "PR 1건 revert. 상세 라우트·핸들러·JSON-LD 빌더·제공자 파일이 모두 본 PR 신규 추가분이므로 revert 후 상세 URL 은 404 를 반환하고 sitemap 에서 제외된다. 공유 모듈을 생성하지 않으므로 W2 컴파일에 영향이 없다. 이미 색인된 상세 URL 은 404 응답으로 자연 제거되며 DB 변경 없음."
done_when:
  - "REQ-1~REQ-8 의 acceptance 명령이 전부 통과하고 apps/web package.json scripts 에 등록됨"
  - "골든셋 40건의 표집 절차·선정 결과와 홀드아웃 20건의 존재(값 비공개)가 PR 본문에 기록됨"
  - "REQ-8 스테이징 스모크 로그가 PR 본문에 첨부됨"
  - "가격 상태 4종 + 충돌 + 요금제 0건 = 5개 픽스처 렌더 스냅샷이 커밋됨"
  - "FORBID-1~6 각각의 탐지 수단이 의도적 위반 픽스처에서 실패하는 것을 확인한 기록이 PR 본문에 첨부"
  - "픽스처가 C7 public_venue 뷰 컬럼 구성 및 F2 visibility ENUM 4값과 zod 로 일치 검증됨"
```
