# W9 — 관측 이벤트 기반 (전환 분모 · 필터 · 코호트 · 리텐션)

> 개정 2판 (2차 감사 `docs/audit/round2-webops.md` W9 절 반영)
> 주요 변경: **`price_block_present` 산출 규칙·기대값 검증 신설**(H4 코호트 축이 무검증이던 문제) ·
> **H5 분모 정의** · 프리페치/RSC 재요청 분모 제외 · 봇 판정 홀드아웃(R-6) · 이벤트 저장 스키마를 F2b 선행 조건 표로 명시 ·
> DB 접근을 F5 경유로 확정

## 선행 조건 — 상류 계약 개정 (미충족 시 착수 금지)

| # | 필요한 것 | 소유 계약 | 현재 상태 | 확인 방법 |
|---|---|---|---|---|
| 1 | `lead_event` 화이트리스트에 `visitor_hash` · `event_props jsonb` 추가 (4종 이벤트 속성 수용) — 또는 동등한 `observability_event` 테이블 | F2b-QUALITY-SCHEMA | **부재** (현 화이트리스트 7컬럼: event_type·surface·venue_id·session_hash·is_bot·event_version·occurred_at) | `packages/db/test/allowed-columns.quality.json` 에 항목 존재 |
| 2 | `event_props` 내부 키 화이트리스트(`venue_id`·`has_editor_report`·`price_block_present`·`tag_ids`·`result_count`)와 PII 키 금지 규칙 | F2b-QUALITY-SCHEMA | 부재 | 스키마 테스트에 키 집합 검사 존재 |
| 3 | 이벤트 INSERT · `editor_report` 보유 여부 조회를 수행하는 쓰기/조회 함수 (`recordEvent`, `getVenueCohortFlags`) | F5-API-LAYER | **부재** (공개 조회 2종만) | `packages/api` export 에 2함수 존재 |
| 4 | `price_block_present` 판정 규칙의 정본 — 상세 화면의 가격 블록 렌더 조건 | W3-VENUE-DETAIL | 미확정 | W3 계약에 렌더 조건 REQ 존재 (§REQ-3 상호 참조) |
| 5 | F6 표시 상태 4값(정정 #10) export | F6-PRICE-STATE | 어휘 불일치 | `packages/price-state` export 값 집합 |

> 3 이 미해소면 `apps/web` 의 DB 직접 접근이 F5 `boundary` job 에 확정적으로 걸린다. **예외를 파지 않는다 — 착수를 중단하고 상신한다.**
> 4·5 는 상신 대상(타 저자 소유). 4 없이 구현하면 H4 코호트 정의가 두 벌이 된다.

```yaml
# ─── 식별 ───────────────────────────────
id:            W9-OBSERVABILITY-EVENTS
dag_id:        W9
title:         관측 이벤트 기반 — 지속 식별자 · 수집 엔드포인트 · detail_view/filter_applied · 리텐션 뷰
workstream:    web
owner_agent:   dev-web

# ─── 존재 이유 ──────────────────────────
traces_to:     [H4, H5, H6, H7]
why:           "이 태스크가 없으면 나머지 계약을 전부 구현해도 출시 후 가설 4개를 판정할 수 없다 — 전환율의 분모, 필터 사용, 리포트 코호트, 재방문 식별자가 어느 계약에도 존재하지 않는다."

# ─── DAG ────────────────────────────────
depends_on:    [W1-SEO-FOUNDATION, F5-API-LAYER, F2b-QUALITY-SCHEMA]
blocks:        [W6-LEAD-TRACKING, O1-METRICS-DASHBOARD]
parallel_with: [W2-DISCOVERY-LIST, W3-VENUE-DETAIL, W5-EDITOR-REPORT]
gate:          [G1, G2, G3]   # W1~W5 와 동일 규약

# ─── 산출물 ─────────────────────────────
deliverable:
  pr_count:    1
  touches:
    - apps/web/src/lib/observability/**
    - apps/web/src/middleware.ts             # 라우트 관측 단일 훅 (W2·W3·W5 파일 미수정)
    - apps/web/src/app/api/events/ingest/route.ts
    - packages/events/schema/**
    - packages/events/views/detail_view_daily.sql
    - packages/events/views/filter_usage_daily.sql
    - packages/events/views/retention_week4.sql
    - ops/alerts/w9-observability.yml        # O3 규약에 따른 파일 단위 룰 소유
    - ops/probes/synthetic-session.mjs
    - apps/web/tests/observability/**
    - apps/web/fixtures/observability/**
  artifacts:
    - "visitor/session 식별자 발급 모듈 (서버 발급 · 솔티드 해시)"
    - "수집 엔드포인트 (오리진 검증 · 레이트리밋 · 스키마 레지스트리 검증)"
    - "서버측 봇 판정기 + 홀드아웃 UA 셋"
    - "이벤트 3종(detail_view · filter_applied · report_view) v1 스키마"
    - "파생 뷰 3종 + 분모 정의"
    - "합성 세션 프로브 + 하트비트 지표 + 알림 룰 1종"

# ─── 요구사항 ───────────────────────────
requirements:
  - id: REQ-1
    statement: >
      식별자는 서버가 발급하며 visitor 는 1st-party 쿠키(만료 400일)·session 은 30분 무활동 만료이고,
      두 값 모두 환경변수 솔트와 결합한 해시 문자열이며 원시 IP·UA 전체 문자열은 저장되지 않는다.
    acceptance: >
      `pnpm test:identity` — (a) Set-Cookie Max-Age == 34560000, (b) 새로고침 후 session 동일 /
      29분 후 동일 / 31분 후 상이, (c) 저장 레코드에 IP·UA 원문이 존재하면 실패, (d) 솔트 미설정 시 기동 실패.

  - id: REQ-2
    statement: >
      업체 상세 라우트 요청 1회당 미들웨어가 detail_view 1건을 발생시키되, `Sec-Purpose: prefetch`
      또는 `RSC` 헤더가 있는 요청은 분모에서 제외되어 이벤트를 발생시키지 않는다.
    acceptance: >
      `pnpm test:e2e:detail-view` — 일반 요청 20회 → 이벤트 20건, prefetch/RSC 헤더 요청 20회 → 0건.

  - id: REQ-3
    statement: >
      detail_view 의 `price_block_present` 는 "해당 업체의 요금제 중 F6 판정이 `confirmed` 인 행이
      1건 이상"일 때 true 이며(W3 가격 블록 렌더 조건과 동일 정의, 선행 조건 #4),
      `has_editor_report` 는 F5 `getVenueCohortFlags` 조회값을 그대로 기록한다.
    acceptance: >
      `pnpm test:cohort-attrs` — 픽스처 4종(요금제 0건 / confirmed 1건 / low_confidence 만 /
      conflict 만)에서 price_block_present 기대값 (false, true, false, false) 4/4 일치,
      리포트 보유·미보유 픽스처에서 has_editor_report 2/2 일치. 상수 반환 구현 시 실패.

  - id: REQ-4
    statement: >
      니즈 태그 쿼리 파라미터가 포함된 리스트 라우트 요청 1회당 filter_applied 1건이 발생하며 속성으로
      (tag_ids[], result_count)를 포함한다.
    acceptance: >
      `pnpm test:e2e:filter-event` — 태그 쿼리 요청 3회 → 3건, tag_ids 가 URL 쿼리와 3/3 일치,
      태그 없는 요청 3회 → 0건.

  - id: REQ-5
    statement: >
      filter_usage_daily 뷰는 (filter_applied 를 1건 이상 발생시킨 세션 수 ÷ is_bot=false 인 전체
      세션 수)를 산출하고, retention_week4 뷰는 (주차 코호트의 4주차 재방문 visitor 수 ÷ 해당 코호트
      visitor 수)를 산출하며 visitor 식별 불가 세션은 양쪽 분모에서 제외되어
      unidentified_sessions 로 별도 집계된다.
    acceptance: >
      골든 쿼리 테스트 `pnpm test:events-views` — 픽스처(전체 세션 200 중 필터 세션 60,
      코호트 100 중 4주차 22, 미식별 15)에서 0.30 / 0.22 / 15 반환.

  - id: REQ-6
    statement: >
      is_bot 은 서버측 결정론적 규칙(UA 패턴 파일 + headless 표식 + visitor 당 분당 60건 초과)으로만
      산출되며, 개발 중 열람하지 않는 홀드아웃 UA 셋(정상 40 · 봇 40, `holdout.lock` 봉인)에서
      오탐률 ≤ 5% · 미탐률 ≤ 10% 를 만족한다.
    acceptance: >
      `pnpm test:bot-classify` — (a) 홀드아웃 80건 판정 결과가 두 임계 안, (b) 클라이언트가
      is_bot=false 를 명시한 봇 UA 요청도 is_bot=true, (c) 정상 UA 40건 전건 is_bot=false
      (전량 봇 판정 시 실패).

  - id: REQ-7
    statement: >
      수집 엔드포인트는 Origin 헤더가 SITE_ORIGIN 과 불일치하면 403, 동일 visitor 기준 분당 60건
      초과면 429 를 반환하고, 60건 이하 정상 요청은 전건 202 를 반환한다.
    acceptance: >
      `pnpm test:ingest-guard` — 오리진 위조 403, 61번째 429, 정상 60건 전건 202.

  - id: REQ-8
    statement: >
      스테이징에서 합성 세션 프로브가 5분 주기로 detail_view 1건과 filter_applied 1건을 발생시키고,
      events_heartbeat_received 지표가 최근 15분 창에서 값 ≥ 1 을 유지한다.
    acceptance: >
      `pnpm test:e2e:events-staging` — 프로브 1회 실행 후 15분 창 값 ≥1 assert, 수집 차단 주입 시
      값 0 으로 관측되어 실패(값이 상수로 고정되면 실패).

# ─── 조건부 금지사항 ────────────────────
forbid:
  - id: FORBID-1
    when:         식별자를 생성하거나 event_props 키를 추가하는 경우
    must_not:     원시 IP · UA 전체 문자열 · 이메일 · 검색어 원문 · 정밀 좌표(소수점 4자리 초과)를 값 또는 해시 입력으로 저장하거나 해시 솔트를 소스코드에 하드코딩
    because:      로그인·동의 절차가 없는 서비스에 식별정보가 쌓이면 수집 근거 없는 보관이 되고, 솔트가 코드에 있으면 유출 시 해시를 되돌릴 수 있어 로그 백업 전체가 개인정보 파일이 된다
    detect:       CI pii-guard — F2b `allowed-columns.quality.json` 의 event_props 키 화이트리스트 대조 + 저장 페이로드 100건의 전화/이메일/IP 정규식 매치 0건 + 솔트 환경변수 로드 단위테스트
    on_violation: block_merge

  - id: FORBID-2
    when:         클라이언트 페이로드에 서버 산출 필드(is_bot · visitor · surface · occurred_at)가 포함되어 수신된 경우
    must_not:     그 값을 저장하거나 집계 입력으로 사용
    because:      봇이 자기가 봇인지 신고하는 구조가 되면 봇 필터·NSM·H4 코호트가 동시에 무의미해지고, 조작된 수치 위에서 수익화 판단이 내려진다
    detect:       통합테스트 — 위조 페이로드 3종(is_bot=false 인 봇 UA · 타인 visitor · 미래 occurred_at)에서 저장값이 서버 산출값으로 덮어써짐을 assert
    on_violation: block_merge

  - id: FORBID-3
    when:         packages/events/schema 의 기존 필드명·타입·enum 의미를 변경하거나 `price_block_present`·`has_editor_report` 의 산출 규칙(REQ-3)을 변경하는 경우
    must_not:     event_version 증가와 파생 뷰의 event_version 필터 없이 동일 뷰에 계속 적재
    because:      같은 이름 다른 의미의 이벤트가 섞이면 H4·H5·H7 시계열이 조용히 어긋나고, 특히 코호트 축의 정의가 바뀌면 전환율 비교가 반대 결론을 낼 수 있는데 원본에 구분자가 없어 사후 분리가 불가능하다
    detect:       CI 잡 event-schema-guard — 스키마·코호트 산출 모듈 diff 대비 version bump 강제 + 파생 뷰 3종의 event_version WHERE 절 존재 검사 + events_by_version 지표 노출 테스트
    on_violation: block_merge

  - id: FORBID-4
    when:         봇 판정 규칙 파일(UA 패턴·임계)을 축소·완화하거나 is_bot=true 이벤트를 처리하는 경우
    must_not:     원본 이벤트를 수집 단계에서 드롭·삭제하거나, 홀드아웃 재실행 없이 규칙을 완화해 머지
    because:      규칙은 운영 중 반드시 조정되는데 원본이 없으면 재집계가 불가능해 지표 정정이 막히고, 자기 픽스처만 보고 완화하면 봇 트래픽이 정상 방문으로 흡수되어 H4·H5 분모가 부풀려진다
    detect:       통합테스트(raw 20건 존재 + 뷰에서만 제외) + CI — 규칙 파일 diff 시 홀드아웃 재실행 결과 첨부와 작성자 외 승인 없으면 exit 1
    on_violation: block_merge

  - id: FORBID-5
    when:         수집 엔드포인트가 5xx·타임아웃·차단 상태인 경우
    must_not:     페이지 렌더를 지연시키거나 링크 이동·필터 적용을 차단하며 재시도 대기
    because:      계측이 실패할 때 제품이 함께 멈추면 측정하려던 행동 자체가 사라지고, 사용자는 반응 없는 사이트로 인지해 H7 재방문까지 동시에 잃는다
    detect:       e2e — ingest 를 500/차단으로 mock 한 상태에서 상세 TTFB 증가 100ms 미만, 필터 결과 렌더 100ms 이내 assert
    on_violation: block_merge

  - id: FORBID-6
    when:         visitor 식별자가 없는(쿠키 차단·최초 진입 실패) 세션을 리텐션·필터 사용률 지표에서 처리하는 경우
    must_not:     session 식별자를 visitor 대체값으로 사용해 분모·분자에 포함
    because:      쿠키 차단 사용자가 매 방문 신규 방문자로 계산되면 4주 재방문율이 구조적으로 과소 추정되어, H7 이 실제로는 통과했는데 기각되거나 그 반대의 판정이 내려진다
    detect:       단위테스트 — visitor NULL 픽스처 15건이 retention_week4·filter_usage_daily 분모에서 제외되고 unidentified_sessions 에 15로 집계됨을 assert
    on_violation: block_merge

# ─── 경계 ───────────────────────────────
out_of_scope:
  - 리드 액션 이벤트·qualified_lead 집계 — W6 소관
  - 지표 시각화·임계 판정 — O1 소관
  - 알림 채널·룰 스키마·데드맨 — O3 소관 (본 계약은 O3 규약에 따라 자기 룰 파일 1개만 소유)
  - 상세·리스트·리포트 화면의 UI 및 라우트 파일 수정 (W2·W3·W5) — 관측은 middleware 단일 훅에서만
  - W1 의 routes·seo·sitemap 모듈 수정 (import 만)
  - DB 스키마 변경 — F2b 소관 (선행 조건 표 #1·#2)
  - packages/api 함수 구현 — F5 소관 (선행 조건 표 #3, 호출만)
  - A/B 실험 할당 · 개인화 · 로그인 기반 식별 · 서드파티 광고 픽셀

rollback: >
  PR revert 시 이벤트 전송·수집 엔드포인트·쿠키 발급이 중단된다. raw 이벤트와 파생 뷰는 DROP 하지 않는다
  (과거 행동은 재수집 불가). 파생 뷰는 읽기 전용 객체라 남겨도 원본에 영향이 없다. W6·O1 이 이미 머지된
  상태라면 O1 → W6 → W9 역순으로 되돌린다.

done_when:
  - REQ-1~REQ-8 acceptance 가 CI 에서 전부 통과
  - FORBID-1~6 각각의 위반 픽스처가 커밋되고 detect 가 차단함을 PR 본문에 기록
  - `price_block_present` 4종 픽스처 기대값 검증 통과 (상수 구현 차단 확인)
  - 홀드아웃 UA 셋이 `holdout.lock` 으로 봉인되고 개발 중 미열람 상태로 커밋
  - 파생 뷰 3종의 분모 정의가 README 에 수식으로 기재되고 O1 이 참조 가능
  - 선행 조건 표 1~5 충족 확인 (미충족 시 착수 금지)
  - touches 밖 파일 변경 0건 (CI path guard 통과)
```
