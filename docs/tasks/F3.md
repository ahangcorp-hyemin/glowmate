# F3 — 디자인 토큰 & 코어 컴포넌트 ⛔ 폐기 (SUPERSEDED)

> 상태: **폐기 2026-08-03.** 이 계약은 개발 에이전트에게 전달되지 않으며 신규 참조를 금지한다.
> 근거: `docs/03-task-dag.md` 정본 ID 레지스트리 — `F3-DESIGN-TOKENS ← 폐기(DS 워크스트림으로 대체). 신규 참조 금지`.
> G5 판정 옵션 A 확정(`docs/ds0-seed-design-실사.md`)으로 토큰 원천이 `@seed-design/css` +
> `@seed-design/tailwind4-theme` 로 바뀌어, 자체 tokens.json 파이프라인을 전제한 본 계약의 REQ 대부분이 무효가 되었다.

## 대체 계약 (Phase 1.5 Design System)

| 신규 | 본 계약에서 이관된 내용 |
|---|---|
| DS1-TOKEN-LAYERS | 토큰 계층·브랜드 팔레트 오버레이 (구 REQ-1) |
| DS3-BASE-WIRING | Pretendard self-host · seed css 연결 · shadcn 초기화 |
| DS4-PRICE-COMPARE-CARD | 회당 단가 비교 카드 · 가격 미공개/저신뢰 표시 (구 REQ-6 · FORBID-3) |
| DS5-CORE-COMPONENTS | 업체 카드 · 니즈 태그 칩 · 필터 바 · 리스트 (구 REQ-5) |
| DS6-A11Y-GATE | 본문 17px · 대비 · 터치타깃 48px · 200% 리플로 CI 강제 (구 REQ-2·3·4·7 · FORBID-2) |
| DS7-PREVIEW-DOCS | 프리뷰/문서화 |

## 참조 정정 필요 (상신)

`depends_on` 또는 `blocks` 에 `F3` / `F3-DESIGN-TOKENS` 를 선언한 계약: **W1 · W2 · W3 · O3**.
→ DS 워크스트림 노드(W1·W2·W3 = `DS5-CORE-COMPONENTS` + `DS6-A11Y-GATE`)로 교체해야 한다.

## 폐기 시 유실 방지 (DS 계약 저자에게 인계)

- **원칙 2.5 짝 REQ**: "고신뢰 입력에서 숫자 가격이 실제로 렌더되는 비율 100%" — 감사 F3 §A 가 지적한
  "항상 '가격 미공개' 렌더가 계약 100% 준수" 경로는 DS4 에서 반드시 다시 막아야 한다.
- **자기차단 해소**: 임계값 파일 최초 생성은 잠금 FORBID 의 예외로 규칙 안에 명시할 것 (감사 P7).
- **라벨은 통제가 아니다**: 임계 변경 승인은 PR 라벨이 아니라 CODEOWNERS 승인 리뷰(승인자 ≠ 작성자)로 검사할 것.
