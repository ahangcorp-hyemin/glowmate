# F4 — 니즈 태그 온톨로지 정의

> 계약 규격: [02-task-contract-spec.md](../02-task-contract-spec.md) §1
> 재작성: 2026-08-03 (1차 감사 **REJECT** 후 재설계 — 존재하지 않는 선행 산출물 참조 제거 · G1 조건부 축 · 원칙 2.5 · R-6 · R-7)

```yaml
# ─── 식별 ───────────────────────────────
id:            F4-NEED-TAG-ONTOLOGY
dag_id:        F4
title:         3050 니즈 태그 온톨로지 (업종 용어 → 통증·생애주기 언어 매핑)
workstream:    foundation
owner_agent:   dev-data

# ─── 존재 이유 ──────────────────────────
traces_to:     [H5, S2]
why:           "업종 용어를 통증·생애주기 언어로 번역한 태그 체계가 확정되지 않으면 C6 자동 부여도,
                W2 필터도, W4 조합 랜딩의 니즈 축도 정의할 수 없다."

# ─── DAG ────────────────────────────────
depends_on:     [F1-REPO-SCAFFOLD, D1a-PROTOCOL, D1b-FIELDWORK, D4-MEDICAL-AD-GUARDRAIL]
               # D1a/D1b 는 03-task-dag.md Phase 0 표의 분할 표기다. 정본 ID 레지스트리에 슬러그가
               # 아직 등재되지 않았으므로, 등재되는 즉시 해당 슬러그로 교체해야 한다(F1 dag-check 대상).
blocks:        [C6-TAG-ASSIGN, W2-DISCOVERY-LIST, W4-COMBO-LANDING]
parallel_with:  [F5-API-LAYER, F6-PRICE-STATE, C1-CRAWLER-CORE]
gate:          G1                    # 판정 정본: docs/gates/G1.md (F1 산출물)

# ─── 산출물 ─────────────────────────────
deliverable:
  pr_count:    1
  touches:
    - data/ontology/need-tags.yaml
    - data/ontology/need-tags.schema.json
    - data/ontology/term-corpus.dev.csv        # 개발셋 70개 업체 용어 원문
    - data/ontology/term-corpus.holdout.csv    # 홀드아웃 30개 업체 (판정 전용)
    - data/ontology/holdout.lock               # 홀드아웃 sha256 잠금
    - data/ontology/snapshots/**               # 용어 원문 페이지 스냅샷
    - data/ontology/labels.dev.csv             # 개발셋 태그 라벨링
    - data/ontology/labels.holdout.csv         # 홀드아웃 태그 라벨링 (최종 판정용)
    - docs/ontology/need-tags.md               # 업종 용어 → 통증·생애주기 언어 대조표
    - tools/ontology-validate/**
    - .github/workflows/ci.yml                 # ontology job 추가만
    - .github/ci-budget.json                   # ontology 예산 항목 추가만
  artifacts:
    - "기계 판독 가능한 니즈 태그 온톨로지 (need-tags.yaml + JSON Schema)"
    - "용어 코퍼스 100개 업체 (개발 70 / 홀드아웃 30, 스냅샷 첨부)"
    - "태그 라벨링 시트 2종 + 홀드아웃 해시 잠금"
    - "검증 스크립트 (CI 진입점) + 위반 픽스처"

# ─── 요구사항 ───────────────────────────
requirements:
  - id: REQ-1
    statement: >
      need-tags.yaml 이 18~28개 태그를 정의하고, 각 태그가 필수 10필드
      (id, slug, label_ko, definition, inclusion_criteria, exclusion_criteria, match_patterns,
      applies_to_axes, aliases, filter_visible)를 모두 채우며 스키마가 additionalProperties:false 다.
    acceptance: "CI job `ontology` — `node tools/ontology-validate --check schema` 가 JSON Schema 검증 통과 + 태그 수 18~28 범위일 때만 exit 0."

  - id: REQ-2
    statement: >
      docs/gates/G1.md 의 verdict 가 proceed 또는 editor_augment 인 축 각각에 대해 applies_to_axes 에
      해당 축을 포함하는 태그가 5개 이상이고, verdict 가 axis_excluded 인 축을 포함하는 태그가 0개다.
    acceptance: >
      `node tools/ontology-validate --check axes` — docs/gates/G1.md 의 per_axis 표를 파싱해 판정.
      verdict 가 undecided 인 축이 하나라도 있으면 `gate_undecided` 로 exit 1 (착수 불가).

  - id: REQ-3
    statement: >
      용어 코퍼스는 D1a 가 잠근 표본 프레임(docs/discovery/D1/sample.csv 의 status=primary 100행)
      전체를 모집단으로 하며, 축·구 층화 후 고정 시드로 개발셋 70 / 홀드아웃 30 으로 분리되고
      각 행이 (venue_id, source_url, term_snippet, snapshot_path) 4필드를 갖는다.
    acceptance: >
      `node tools/ontology-validate --check corpus` — (a) 두 CSV 의 venue_id 합집합이 sample.csv 의
      primary 100행과 정확히 일치·교집합 0, (b) 분할 스크립트를 고정 시드로 재실행한 결과가 커밋본과 동일,
      (c) 전 행의 snapshot_path 파일이 실재하고 term_snippet 이 그 파일의 부분문자열.

  - id: REQ-4
    statement: "각 태그의 aliases 가 3개 이상이며, 모든 alias 가 term-corpus.dev.csv 의 행 id 를 evidence 로 1개 이상 참조한다."
    acceptance: "`node tools/ontology-validate --check evidence` — alias 수 < 3 이거나 evidence 가 비었거나 개발셋 CSV 에 없는 행 id 를 참조하면 누락 목록과 함께 exit 1."

  - id: REQ-5
    statement: "홀드아웃 30개 업체 중 최소 1개 태그를 부여할 수 있는 업체 비율이 60% 이상이다 (최종 커버리지 판정)."
    acceptance: "`node tools/ontology-validate --check coverage` — labels.holdout.csv 집계 결과가 0.60 미만이면 실측치와 함께 exit 1. 개발셋으로 이 검사를 대체하면 실패로 간주한다."

  - id: REQ-6
    statement: >
      모든 match_patterns 가 정규식으로 컴파일되고, 홀드아웃 코퍼스에 적용했을 때 오탐
      (해당 태그 라벨이 없는 행에 매치)이 전체 매치의 20% 이하다.
    acceptance: "`node tools/ontology-validate --check patterns` — 컴파일 실패 1건이라도 있으면 exit 1, 오탐률 > 0.20 이면 태그 id 와 함께 exit 1."

  - id: REQ-7
    statement: >
      filter_visible: true 인 태그가 진행 축(REQ-2 기준)당 4개 이상이고 전체 10개 이상이며,
      display_order 가 1부터 시작하는 연속 정수로 중복 없이 부여된다.
    acceptance: "`node tools/ontology-validate --check filters` — 축당 4 미만이거나 전체 10 미만이거나 display_order 집합 ≠ [1..n] 이면 exit 1."

  - id: REQ-8
    statement: >
      applies_to_axes 에 medical_wellness 를 포함하는 모든 태그가 legal_flag: true 와
      guardrail_ref(정규식 `^MED-[A-Z]-\d{2}$`)를 가지며, 참조된 rule_id 가 docs/discovery/D4/ 의
      4개 yaml 에 실재하고 그 항목의 status 가 advisory 가 아니다.
    acceptance: "`node tools/ontology-validate --check medical` — 조건 미충족 태그 1건이라도 있으면 태그 id·rule_id 와 함께 exit 1."

# ─── 조건부 금지사항 ────────────────────
forbid:
  - id: FORBID-1
    when: "등록하려는 태그 또는 alias 가 term-corpus.dev.csv 의 어느 행에서도 관측되지 않은 표현인 경우"
    must_not: "추측·브레인스토밍으로 태그나 별칭을 추가"
    because: >
      근거 없는 태그는 C6 자동 부여에서 매칭 0건으로 남아 W2 필터에 항상 결과 0건인 칩을 만든다.
      사용자가 빈 필터를 한 번 누르면 필터 기능 전체를 신뢰하지 않게 되고, H5(필터 사용률 25%)의
      저조한 수치가 "가설이 틀렸다"인지 "태그가 허구였다"인지 영원히 구분되지 않는다.
    detect: "CI job `ontology` — `--check evidence` 가 전 alias 의 evidence 행 id 를 개발셋 CSV 및 스냅샷 파일과 대조(REQ-4·REQ-3c). 위반 픽스처 `tools/ontology-validate/fixtures/no-evidence-alias.yaml` 이 반드시 실패하는지 확인하는 메타테스트 포함."
    on_violation: block_merge

  - id: FORBID-2
    when: "applies_to_axes 에 medical_wellness 를 포함하는 태그의 label_ko · definition · inclusion_criteria 문구를 작성할 때"
    must_not: "docs/discovery/D4/forbidden_lexicon.yaml 의 pattern 에 매치되는 표현(치료효과 보장·최상급·유인 표현) 사용"
    because: >
      온톨로지 문구는 W2 필터 칩 라벨과 W4 조합 랜딩 제목·메타로 그대로 복제되어 수천 페이지에 동시 노출된다.
      의료광고법 위반 표현이 한 번 들어가면 사이트 전역이 동시에 위반이 되고, 색인된 뒤에는 수정해도
      검색결과 캐시에 남는다.
    detect: "CI job `ontology` — `--check medical-lexicon` 이 D4 정본 yaml 의 pattern 사전을 로드해 3개 문구 필드를 검사. 사전 파일이 없으면 exit 1(파일 부재를 통과로 처리 금지)."
    on_violation: block_merge

  - id: FORBID-3
    when: "base 브랜치(origin/main)에 이미 존재하는 태그 id 를 유지한 채 definition 또는 inclusion_criteria 또는 match_patterns 를 변경하거나, 그 id 를 다른 개념에 재사용하는 경우 (최초 도입 PR 에서는 base 가 없으므로 발동하지 않는 유지보수 규칙이다)"
    must_not: "id 를 고정한 채 의미 변경 (deprecated: true 표시 후 신규 id 발급으로 처리)"
    because: >
      태그 id 는 C6 가 기록한 venue_need_tag 행과 W4 랜딩 URL(/gangnam/pilates/lower-back)의 키다.
      의미만 바뀌면 이미 색인된 페이지의 제목과 그 페이지가 나열하는 업체 목록이 조용히 어긋나고,
      데이터 정합성 검사로는 아무 오류도 감지되지 않는다.
    detect: "CI job `ontology` — `--check id-stability` 가 base 리비전의 need-tags.yaml 과 대조해 (id 동일 && 3개 필드 해시 변경 && deprecated 미표기) 조합 발견 시 exit 1"
    on_violation: block_merge

  - id: FORBID-4
    when: "성별 표현(여성전용·남성전용·women·men)을 온톨로지에 도입하는 경우"
    must_not: "최상위 필드(gender·target_gender·for_women 등)로 표현하거나, 성별 표현을 포함하는 태그를 2개 이상 등록"
    because: >
      PRD 확정 판단은 "카피는 좁게, 스키마는 넓게"다. 성별이 구조 필드가 되거나 태그가 성별로 분화되면
      사실상 성별이 축이 되어, 4050 남성 세그먼트 확장 시 F2a 스키마와 이미 색인된 W4 URL 까지
      파괴적 마이그레이션이 전파된다.
    detect: "CI job `ontology` — (a) 스키마 허용 키 화이트리스트에 성별 키 부재 assert, (b) label_ko·aliases 에 성별 표현 정규식이 매치되는 태그 수가 2 이상이면 exit 1"
    on_violation: block_merge

  - id: FORBID-5
    when: "코퍼스 100건 합산 라벨에서 부여 가능 업체가 3건 미만인 태그를 다룰 때"
    must_not: "그 태그에 filter_visible: true 를 부여 (정의는 남기되 노출 목록에서 제외)"
    because: >
      결과 0~2건인 필터 칩은 첫 클릭에서 "데이터가 없는 사이트"라는 인상을 만들고 이후 다른 필터도
      시도되지 않는다. 노출 태그 목록은 W2·W4 의 입력이므로 잘못 켜진 채 배포되면 빈 랜딩이 대량 색인된다.
    detect: "CI job `ontology` — `--check filters` 가 filter_visible:true 인 각 태그의 라벨 카운트 ≥ 3 을 검사, 미달 시 태그 id 와 함께 exit 1"
    on_violation: block_merge

  - id: FORBID-6
    when: "이 PR 의 diff 가 docs/discovery/** · data/legal/** · scripts/discovery/** 경로의 파일을 생성하거나 수정하는 경우"
    must_not: "선행 태스크(D1a·D1b·D4) 소유 산출물을 만들어 넣거나 고치는 것 (부재 시 작업을 중단하고 해당 계약에 변경을 요청한다)"
    because: >
      선행 파일이 없을 때 가장 쉬운 우회는 직접 만들어 채우는 것이고, 그 순간 의료광고 규칙과 표본 프레임이
      개발 에이전트의 추측으로 대체된다. 그 추측 위에서 G1·G3 판정이 내려지면 게이트는 자기가 만든 답을 읽는다.
    detect: "CI job `ontology` — `git diff --name-only origin/main` 이 세 경로 중 하나라도 포함하면 exit 1 (읽기는 허용, 쓰기만 차단)"
    on_violation: block_merge

# ─── 경계 ───────────────────────────────
out_of_scope:
  - "태그 자동 부여 실행·LLM 프롬프트·부여 임계값 (C6)"
  - "need_tag 테이블 행 INSERT (C6) — 본 태스크는 데이터 파일까지"
  - "태그 간 계층·함의 관계(implies/parent) 정의 — MVP 비대상"
  - "필터 UI 컴포넌트·인터랙션 (W2) · 조합 랜딩 URL 규칙 (W4)"
  - "가격·카테고리 분류 체계 (C4 · F2a)"
  - "D1/D4 산출물 생성·수정 (FORBID-6)"
  - "영문·다국어 라벨"

rollback: >
  산출물은 데이터·문서 파일과 검증 스크립트뿐이며 런타임 상태를 변경하지 않는다.
  `git revert <merge-sha>` 로 전량 원복되고 C6 미착수 시점이면 need_tag 테이블에 남는 행이 없다.
  C6 착수 후에는 revert 대신 해당 태그를 `deprecated: true` 로 표시한다(FORBID-3).
  ontology job 을 브랜치 보호 필수 체크에서 함께 해제한다.

done_when:
  - "선행 입력 3종(docs/discovery/D1/sample.csv · docs/discovery/D1/ledger.csv · docs/discovery/D4/*.yaml)이 리포에 실재함을 확인하고, 부재 시 착수하지 않았음을 PR 설명에 기록"
  - "docs/gates/G1.md 의 per_axis verdict 가 전 축 undecided 가 아님을 확인"
  - "`node tools/ontology-validate` 8개 --check 서브커맨드 전부 exit 0"
  - "홀드아웃 커버리지 실측치·축별 태그 수·오탐률이 CI 로그에 출력됨"
  - "위반 픽스처 5종(근거 없는 alias / 금칙어 medical 태그 / 3건 미만 filter_visible / 성별 태그 2개 / docs/discovery 경로 수정)이 각각 검증 스크립트를 실패시킴을 확인"
  - "홀드아웃 CSV 가 개발 중 열람되지 않았음을 holdout.lock sha256 과 커밋 이력으로 확인"
  - "docs/ontology/need-tags.md 에 태그 전건의 업종 용어 → 통증·생애주기 언어 대조표가 존재"
```
