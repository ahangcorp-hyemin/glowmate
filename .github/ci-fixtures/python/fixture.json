{
  "job": "python",
  "label": "⑥ pytest 실패",
  "expect": [
    "FAILED services/crawler/tests/",
    "FAILED tests/",
    "=== FAILURES ==="
  ],
  "branch": "ci-fixture/python",
  "overlay": "overlay/",
  "note": "ruff 를 통과하고 pytest 에서만 red 가 되어야 한다. `assert` 처럼 일반적인 토큰은 ruff 실패로도 매칭되어 귀속이 공허해진다(검수 지적)."
}
